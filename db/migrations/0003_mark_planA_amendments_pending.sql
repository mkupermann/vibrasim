-- Plan A — keep R1, R2, PHASE3-R1 status as 'proposed' (no change to seed)
-- This migration is a no-op placeholder reserving the slot for when Plan A
-- merges and we mark them 'implemented'.
SELECT 1;
