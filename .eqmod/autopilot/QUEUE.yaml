# EQMOD Research Queue — Vacation 2026-05-14 .. 2026-05-28
#
# Multi-attempt flux-substrate emergence sprint. Replaces the prior
# Maintenance queue (preserved at QUEUE.maintenance.yaml.bak — AUTO-2 and
# AUTO-3 stay queued there for post-vacation pickup).
#
# Mission: bottom-up emergence of binding from a single principle
# (energy flux), not from engineered binding rules. Each item advances
# the flux substrate by one pre-registered stage. NULL is a valid verdict.
#
# Briefs point at existing plan documents under docs/superpowers/plans/.
# Acceptance is locked at in_progress (pre-commit hook blocks edits to
# the preregistered_acceptance: block).
#
# Status-Übergänge sind Postflight-Maschinenvorgang — Claude darf Status
# nicht direkt editieren (pre-commit hook blockt).
#
# Status-Werte:
#   queued       - noch nicht angerührt
#   in_progress  - aktuelle Session arbeitet daran (1 Item gleichzeitig)
#   passed       - acceptance erfüllt
#   null         - acceptance nicht erfüllt nach fair attempt, postmortem in LOGBOOK
#   failed       - 3 attempts ohne pass, written-off
#   blocked      - HUMAN_NEEDED, wartet auf Michael

window:
  start: 2026-05-14
  end:   2026-05-28
  default_model: claude-opus-4-7
  per_session_max_turns: 80
  per_session_wallclock_hours: 4

items:
  - id: R-1
    title: flux F1c — thermal dynamics + T2 Bénard convection
    brief: docs/superpowers/plans/2026-05-11-flux-substrate-F1c.md
    preregistered_acceptance:
      - "tests/flux/test_thermal.py PASSES (6 new unit tests per plan Task 2)"
      - "tests/flux/test_benard.py::test_T2_benard_horizontal_wavelength PASSES (T2 acceptance per plan Task 5)"
      - "tests/flux/test_conservation.py PASSES (T1 holds with buoyancy + damping active)"
      - "tests/flux/test_crystallization.py PASSES (T3 unaffected by thermal layer)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected by thermal layer)"
      - "pytest -m 'not slow' PASSES (legacy regression baseline holds)"
    time_budget_hours: 12
    status: passed
    attempts: 1
    last_session: "2026-05-14T00:11:39"
    blockers: []
    created: 2026-05-13

  - id: R-1b
    title: flux F1 — architectural extension (horizontal force / pressure surrogate)
    brief: docs/superpowers/plans/2026-05-14-flux-F1-robustness.md
    preregistered_acceptance:
      - "tests/flux/test_horizontal_dynamics.py::test_horizontal_force_responds_to_T_gradient PASSES (NEW test the session writes; >=80% of alive quanta acquire non-zero horizontal velocity under a horizontal T gradient)"
      - "tests/flux/test_horizontal_dynamics.py::test_horizontal_force_zero_when_no_gradient PASSES (NEW; mean horizontal velocity < 0.01 under uniform T — confirms force fires only on real gradients)"
      - "tests/flux/test_conservation.py PASSES (T1 still holds with new force active)"
      - "tests/flux/test_crystallization.py PASSES (T3 unaffected)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
      - "tests/flux/test_thermal.py PASSES (existing thermal unit tests unaffected)"
      - "tests/flux/test_dynamics.py PASSES (dynamics smoke unaffected)"
    time_budget_hours: 12
    status: passed
    attempts: 2
    last_session: "2026-05-14T14:13:13"
    blockers: ["R-1 passed (F1c baseline imported to main); architectural fix replaces lucky-seed Bénard pass"]
    created: 2026-05-14

  - id: R-1c
    title: flux F1 — Bénard multi-seed robustness audit
    brief: docs/superpowers/plans/2026-05-14-flux-F1-robustness.md
    preregistered_acceptance:
      - "tests/flux/test_benard_robustness.py::test_T2_passes_on_at_least_8_of_10_seeds PASSES (NEW; parametrise across seeds 7,13,21,42,100,137,256,314,500,1000; at least 8/10 must satisfy the wavelength check)"
      - "tests/flux/test_benard_robustness.py::test_T2_FFT_SNR_above_3 PASSES (NEW; mean FFT signal-to-noise ratio across passing seeds must be >=3.0 — was 1.5 in original F1c)"
      - "tests/flux/test_benard_robustness.py::test_T2_negative_control_fails_all_10_seeds PASSES (NEW; with buoyancy_g=0, ALL 10 seeds must FAIL the wavelength check — substrate must not produce horizontal structure from RNG alone)"
      - "tests/flux/test_benard.py PASSES (original seed=42 acceptance unchanged)"
    time_budget_hours: 12
    status: null
    attempts: 1
    last_session: "2026-05-15T08:32:49.373331"
    blockers: ["R-1b must reach status=passed first"]
    created: 2026-05-14

  - id: R-1c-bis
    title: flux F1 — Bénard architecture variant 2 (return-flow injector at cold ceiling)
    brief: docs/superpowers/plans/2026-05-14-flux-F1-robustness.md
    preregistered_acceptance:
      - "tests/flux/test_benard_robustness.py::test_T2_passes_on_at_least_8_of_10_seeds PASSES (same 10-seed grid as R-1c; threshold ≥8/10; no retuning)"
      - "tests/flux/test_benard_robustness.py::test_T2_FFT_SNR_above_3 PASSES (mean SNR ≥3.0 across passing seeds — R-1c saw NULL on 0/10 passes)"
      - "tests/flux/test_benard_robustness.py::test_T2_negative_control_fails_all_10_seeds PASSES (buoyancy_g=0 must FAIL on ALL 10 seeds — R-1c had 2/10 spurious passes here, this gate is the discriminating-metric check)"
      - "tests/flux/test_benard.py PASSES (original seed=42 acceptance; session must remove the @pytest.mark.slow marker that was added 2026-05-15)"
      - "tests/flux/test_horizontal_dynamics.py PASSES (R-1b's pressure-gradient tests; may stay or be modified per the session's architectural decision, but cannot regress)"
      - "tests/flux/test_conservation.py PASSES (T1 holds)"
      - "tests/flux/test_crystallization.py PASSES (T3 unaffected)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
    time_budget_hours: 16
    status: null
    attempts: 1
    last_session: "2026-05-15T12:01:59.081816"
    blockers: ["R-1c NULL — adaptive iteration per user mandate 2026-05-15 10:25"]
    created: 2026-05-15

  - id: R-1d
    title: flux F1 — Phase-1 joint robustness gate
    brief: docs/superpowers/plans/2026-05-14-flux-F1-robustness.md
    preregistered_acceptance:
      - "tests/flux/test_phase1_robustness.py::test_all_T_tests_pass_jointly_on_8_of_10_seeds PASSES (NEW; T1+T2+T3+T4 all pass on the same seed for >=8 of the 10 seeds; identical parameter envelope across seeds, no per-seed tweaks)"
      - "tests/flux/test_phase1_robustness.py::test_no_T_test_regresses_under_extension PASSES (NEW; with R-1b extension active, each T-test passes on at least as many seeds as it did without R-1b — no regression)"
    time_budget_hours: 8
    status: null
    attempts: 1
    last_session: "2026-05-15T15:07:55.519505"
    blockers: ["R-1c must reach status=passed first — final gate before F2 work resumes"]
    created: 2026-05-14

  - id: R-1c-tris
    title: flux F1 — Bénard architecture iter-2 (smoothing + density-boost against Poisson noise)
    brief: docs/superpowers/plans/2026-05-15-flux-F1-robustness-iter2.md
    preregistered_acceptance:
      - "tests/flux/test_benard_robustness.py::test_T2_passes_on_at_least_8_of_10_seeds PASSES (same 10-seed grid as R-1c, threshold ≥8/10, no retuning)"
      - "tests/flux/test_benard_robustness.py::test_T2_FFT_SNR_above_3 PASSES (mean SNR ≥3.0 across passing seeds — R-1c saw NULL on 0/10)"
      - "tests/flux/test_benard_robustness.py::test_T2_negative_control_fails_all_10_seeds PASSES (buoyancy_g=0 must FAIL ALL 10 — R-1c had 2/10 spurious passes)"
      - "tests/flux/test_benard.py PASSES (seed=42 original test; session must remove @pytest.mark.slow marker added 2026-05-15)"
      - "tests/flux/test_horizontal_dynamics.py PASSES (R-1b pressure-grad tests or successors)"
      - "tests/flux/test_conservation.py PASSES (T1 holds)"
      - "tests/flux/test_crystallization.py PASSES (T3 unchanged by this item)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
    time_budget_hours: 12
    status: queued
    attempts: 0
    blockers: ["R-1c-bis NULL — third Bénard architecture variant (smoothing + density-boost against Poisson noise), per user mandate 2026-05-15 15:50"]
    created: 2026-05-15

  - id: R-1d-T3
    title: flux F1 — Crystallisation (T3) robustness audit + fix
    brief: docs/superpowers/plans/2026-05-15-flux-F1-robustness-iter2.md
    preregistered_acceptance:
      - "tests/flux/test_crystallization_robustness.py::test_T3_passes_on_at_least_8_of_10_seeds PASSES (NEW: parametrise across 10 seeds, threshold ≥8/10)"
      - "tests/flux/test_crystallization_robustness.py::test_T3_negative_control_fails_all_10_seeds PASSES (NEW: matched-wallclock no-binding control must FAIL ALL 10)"
      - "tests/flux/test_crystallization.py PASSES (seed=42 original test still passes)"
      - "tests/flux/test_conservation.py PASSES (T1 unaffected)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
    time_budget_hours: 12
    status: queued
    attempts: 0
    blockers: ["R-1d found T3 is 2/10 lucky — independent of T2 Bénard work; addresses the second fragility class"]
    created: 2026-05-15

  - id: R-2
    title: flux F2 plan — cochlea + synthesis + first audio input
    brief: docs/superpowers/specs/2026-05-10-flux-substrate-design.md
    preregistered_acceptance:
      - "tests/test_research_plan_structure.py::test_F2_plan_exists_and_well_formed PASSES (verifies file at docs/superpowers/plans/<date>-flux-substrate-F2.md exists with required headings: Acceptance contract, File structure (locked decisions), Open calibration choices, Task N sections, at least one tests/flux/test_*.py acceptance target, reference to spec section 5)"
      - "Plan must declare these specific pytest acceptance paths so R-3 can verify them: tests/flux/test_cochlea.py and tests/flux/test_synthesis.py (this is the contract between R-2 and R-3 — R-2 picks the design, R-3 implements against it)"
      - "Plan must be compatible with autonomous-prototype-build skill (checkbox tasks, no free-form prose-only implementation)"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-14T08:08:22"
    blockers: ["R-1 must reach status=passed first"]
    created: 2026-05-13

  - id: R-3
    title: flux F2 — cochlea + synthesis implementation
    brief: docs/superpowers/plans/2026-05-14-flux-substrate-F2.md
    preregistered_acceptance:
      - "tests/flux/test_cochlea.py PASSES (pre-registered by R-2 as F2's cochlea acceptance test)"
      - "tests/flux/test_synthesis.py PASSES (pre-registered by R-2 as F2's synthesis acceptance test)"
      - "tests/flux/test_conservation.py PASSES (T1 conservation still holds with new cochlea + synthesis layers)"
      - "tests/flux/test_benard.py PASSES (T2 from R-1 still holds — F2 cochlea must not break thermal layer)"
      - "tests/flux/test_crystallization.py PASSES (T3 still holds)"
      - "tests/flux/test_decay.py PASSES (T4 still holds)"
    time_budget_hours: 16
    status: queued
    attempts: 0
    blockers: ["R-2 must reach status=passed first"]
    created: 2026-05-13

  - id: R-4
    title: flux F3 plan — learning-as-flux-reconfiguration
    brief: docs/superpowers/specs/2026-05-10-flux-substrate-design.md
    preregistered_acceptance:
      - "tests/test_research_plan_structure.py::test_F3_plan_exists_and_well_formed PASSES (verifies file at docs/superpowers/plans/<date>-flux-substrate-F3.md exists with required headings AND references the flux principle AND pre-registers a negative control)"
      - "Plan must declare these specific pytest acceptance paths so R-5 can verify them: tests/flux/test_learning.py and tests/flux/test_learning_negative_control.py (R-4 picks the learning rule, R-5 implements + runs falsification)"
      - "Plan defines a learning rule derived from the flux principle (NOT an STDP/BTSP/Hebbian re-implementation by another name)"
    time_budget_hours: 4
    status: queued
    attempts: 0
    blockers: ["R-3 must reach status=passed first"]
    created: 2026-05-13

  - id: R-5
    title: flux F3 — learning implementation + falsification run
    brief: docs/superpowers/specs/2026-05-10-flux-substrate-design.md
    preregistered_acceptance:
      - "tests/flux/test_learning.py PASSES (pre-registered by R-4 as F3 learning emergence acceptance — substrate exposed to a repeated input pattern develops measurable pattern-specific structure)"
      - "tests/flux/test_learning_negative_control.py PASSES (pre-registered by R-4 as F3 negative control — the same test on a matched-wallclock substrate WITHOUT input must show the substrate does NOT develop pattern-specific structure; verdict logic per F3 plan threshold)"
      - "tests/flux/test_conservation.py PASSES (T1 still holds with learning layer active)"
      - "tests/flux/test_benard.py PASSES (T2 still holds)"
      - "tests/flux/test_crystallization.py PASSES (T3 still holds)"
      - "tests/flux/test_decay.py PASSES (T4 still holds)"
    time_budget_hours: 24
    status: queued
    attempts: 0
    blockers: ["R-4 must reach status=passed first"]
    created: 2026-05-13

  # ----- Training phase: only meaningful if R-5 (F3 learning) actually passed.
  # If F3 NULLed, these items will themselves NULL (their pytest targets depend
  # on the learning layer being live). That is the intended fail-shut behavior.
  # User instruction (2026-05-13 23:18): once the substrate works, also train it.

  - id: R-6
    title: training plan — expose flux substrate to a real English audio corpus
    brief: docs/superpowers/specs/2026-05-10-flux-substrate-design.md
    preregistered_acceptance:
      - "tests/test_research_plan_structure.py::test_training_EN_plan_exists_and_well_formed PASSES (verifies file at docs/superpowers/plans/<date>-flux-training-EN.md exists with: Acceptance contract, File structure (locked decisions), Open calibration choices, Task N sections, at least one tests/flux/test_*.py acceptance target, AND explicit pre-registered negative control + corpus specification)"
      - "Plan must declare these specific pytest acceptance paths so R-8 can verify them: tests/flux/test_training_run.py and tests/flux/test_training_negative_control.py"
      - "Plan body must specify: a single-language English corpus (no German, no French — per user 2026-05-13), Stage 1 (single-narrator audiobook) and Stage 2 (single YouTuber) and a Stage 4 SUBSTITUTE — Stage 3 multi-speaker remains out of vacation scope"
      - "Plan body must specify the Stage 4 substitute: >= 30 min single-speaker English recording from a public-domain or CC-licensed source, distinct speaker from Stage 1 and Stage 2 narrators (user delegated substitution 2026-05-13 23:38 — user on vacation, cannot record own voice). Plan must justify the choice with one paragraph on the acoustic or distributional difference between the Stage 4 substitute and Stages 1-2 (otherwise the substrate is just hearing 3 audiobook narrators with no real variation)" 
      - "Plan body must reference the F3 learning rule produced by R-5 by name and explain how the corpus exercises it"
    time_budget_hours: 4
    status: queued
    attempts: 0
    blockers: ["R-5 must reach status=passed first"]
    created: 2026-05-13

  - id: R-7
    title: training corpus build — fetch English Stage 1+2 sources, validate, manifest
    brief: docs/superpowers/specs/2026-05-10-flux-substrate-design.md
    preregistered_acceptance:
      - "tests/flux/test_training_corpus_valid.py PASSES (corpus YAML at corpus.training-EN.yaml exists, parses, all listed audio assets exist on disk, total Stage 1 duration >= 60 min, total Stage 2 duration >= 120 min, sample-rate normalised to 16 kHz mono)"
      - "tests/flux/test_training_corpus_manifest.py PASSES (manifest written at ~/.eqmod/training/EN/manifest.json with: per-stage durations, source URLs, sha256 of each file)"
    time_budget_hours: 4
    status: queued
    attempts: 0
    blockers: ["R-6 must reach status=passed first"]
    created: 2026-05-13

  - id: R-8
    title: training run — expose substrate to English corpus, measure emergence vs negative control
    brief: docs/superpowers/specs/2026-05-10-flux-substrate-design.md
    preregistered_acceptance:
      - "tests/flux/test_training_run.py PASSES (pre-registered by R-6 — substrate exposed to the English corpus develops the metric defined in the F3 + training plans above its threshold)"
      - "tests/flux/test_training_negative_control.py PASSES (pre-registered by R-6 — matched-wallclock substrate WITHOUT the English corpus must NOT develop the metric above threshold; verdict is PASS only when trained > control by the plan-specified margin)"
      - "docs/flux/phase-log.md has a training-run-close entry with measured numbers (substrate metric, control metric, margin, plan threshold) committed"
      - "LOGBOOK.md has the verdict + interpretation paragraph appended"
    time_budget_hours: 24
    status: queued
    attempts: 0
    blockers: ["R-7 must reach status=passed first"]
    created: 2026-05-13
