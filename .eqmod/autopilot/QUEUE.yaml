# EQMOD Research Queue — Vacation 2026-05-14 .. 2026-05-28
#
# Multi-attempt flux-substrate emergence sprint. Replaces the prior
# Maintenance queue (preserved at QUEUE.maintenance.yaml.bak — AUTO-2 and
# AUTO-3 stay queued there for post-vacation pickup).
#
# Mission: bottom-up emergence of binding from a single principle
# (energy flux), not from engineered binding rules. Each item advances
# the flux substrate by one pre-registered stage. NULL is a valid verdict.
#
# Briefs point at existing plan documents under docs/superpowers/plans/.
# Acceptance is locked at in_progress (pre-commit hook blocks edits to
# the preregistered_acceptance: block).
#
# Status-Übergänge sind Postflight-Maschinenvorgang — Claude darf Status
# nicht direkt editieren (pre-commit hook blockt).
#
# Status-Werte:
#   queued       - noch nicht angerührt
#   in_progress  - aktuelle Session arbeitet daran (1 Item gleichzeitig)
#   passed       - acceptance erfüllt
#   null         - acceptance nicht erfüllt nach fair attempt, postmortem in LOGBOOK
#   failed       - 3 attempts ohne pass, written-off
#   blocked      - HUMAN_NEEDED, wartet auf Michael

window:
  start: 2026-05-14
  end:   2026-05-28
  default_model: claude-opus-4-7
  per_session_max_turns: 80
  per_session_wallclock_hours: 4

items:
  - id: R-1
    title: flux F1c — thermal dynamics + T2 Bénard convection
    brief: docs/superpowers/plans/2026-05-11-flux-substrate-F1c.md
    preregistered_acceptance:
      - "tests/flux/test_thermal.py PASSES (6 new unit tests per plan Task 2)"
      - "tests/flux/test_benard.py::test_T2_benard_horizontal_wavelength PASSES (T2 acceptance per plan Task 5)"
      - "tests/flux/test_conservation.py PASSES (T1 holds with buoyancy + damping active)"
      - "tests/flux/test_crystallization.py PASSES (T3 unaffected by thermal layer)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected by thermal layer)"
      - "pytest -m 'not slow' PASSES (legacy regression baseline holds)"
    time_budget_hours: 12
    status: passed
    attempts: 1
    last_session: "2026-05-14T00:11:39"
    blockers: []
    created: 2026-05-13

  - id: R-1b
    title: flux F1 — architectural extension (horizontal force / pressure surrogate)
    brief: docs/superpowers/plans/2026-05-14-flux-F1-robustness.md
    preregistered_acceptance:
      - "tests/flux/test_horizontal_dynamics.py::test_horizontal_force_responds_to_T_gradient PASSES (NEW test the session writes; >=80% of alive quanta acquire non-zero horizontal velocity under a horizontal T gradient)"
      - "tests/flux/test_horizontal_dynamics.py::test_horizontal_force_zero_when_no_gradient PASSES (NEW; mean horizontal velocity < 0.01 under uniform T — confirms force fires only on real gradients)"
      - "tests/flux/test_conservation.py PASSES (T1 still holds with new force active)"
      - "tests/flux/test_crystallization.py PASSES (T3 unaffected)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
      - "tests/flux/test_thermal.py PASSES (existing thermal unit tests unaffected)"
      - "tests/flux/test_dynamics.py PASSES (dynamics smoke unaffected)"
    time_budget_hours: 12
    status: passed
    attempts: 2
    last_session: "2026-05-14T14:13:13"
    blockers: ["R-1 passed (F1c baseline imported to main); architectural fix replaces lucky-seed Bénard pass"]
    created: 2026-05-14

  - id: R-1c
    title: flux F1 — Bénard multi-seed robustness audit
    brief: docs/superpowers/plans/2026-05-14-flux-F1-robustness.md
    preregistered_acceptance:
      - "tests/flux/test_benard_robustness.py::test_T2_passes_on_at_least_8_of_10_seeds PASSES (NEW; parametrise across seeds 7,13,21,42,100,137,256,314,500,1000; at least 8/10 must satisfy the wavelength check)"
      - "tests/flux/test_benard_robustness.py::test_T2_FFT_SNR_above_3 PASSES (NEW; mean FFT signal-to-noise ratio across passing seeds must be >=3.0 — was 1.5 in original F1c)"
      - "tests/flux/test_benard_robustness.py::test_T2_negative_control_fails_all_10_seeds PASSES (NEW; with buoyancy_g=0, ALL 10 seeds must FAIL the wavelength check — substrate must not produce horizontal structure from RNG alone)"
      - "tests/flux/test_benard.py PASSES (original seed=42 acceptance unchanged)"
    time_budget_hours: 12
    status: None
    attempts: 1
    last_session: "2026-05-15T08:32:49.373331"
    blockers: ["R-1b must reach status=passed first"]
    created: 2026-05-14

  - id: R-1c-bis
    title: flux F1 — Bénard architecture variant 2 (return-flow injector at cold ceiling)
    brief: docs/superpowers/plans/2026-05-14-flux-F1-robustness.md
    preregistered_acceptance:
      - "tests/flux/test_benard_robustness.py::test_T2_passes_on_at_least_8_of_10_seeds PASSES (same 10-seed grid as R-1c; threshold ≥8/10; no retuning)"
      - "tests/flux/test_benard_robustness.py::test_T2_FFT_SNR_above_3 PASSES (mean SNR ≥3.0 across passing seeds — R-1c saw NULL on 0/10 passes)"
      - "tests/flux/test_benard_robustness.py::test_T2_negative_control_fails_all_10_seeds PASSES (buoyancy_g=0 must FAIL on ALL 10 seeds — R-1c had 2/10 spurious passes here, this gate is the discriminating-metric check)"
      - "tests/flux/test_benard.py PASSES (original seed=42 acceptance; session must remove the @pytest.mark.slow marker that was added 2026-05-15)"
      - "tests/flux/test_horizontal_dynamics.py PASSES (R-1b's pressure-gradient tests; may stay or be modified per the session's architectural decision, but cannot regress)"
      - "tests/flux/test_conservation.py PASSES (T1 holds)"
      - "tests/flux/test_crystallization.py PASSES (T3 unaffected)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
    time_budget_hours: 16
    status: None
    attempts: 1
    last_session: "2026-05-15T12:01:59.081816"
    blockers: ["R-1c NULL — adaptive iteration per user mandate 2026-05-15 10:25"]
    created: 2026-05-15

  - id: R-1d
    title: flux F1 — Phase-1 joint robustness gate
    brief: docs/superpowers/plans/2026-05-14-flux-F1-robustness.md
    preregistered_acceptance:
      - "tests/flux/test_phase1_robustness.py::test_all_T_tests_pass_jointly_on_8_of_10_seeds PASSES (NEW; T1+T2+T3+T4 all pass on the same seed for >=8 of the 10 seeds; identical parameter envelope across seeds, no per-seed tweaks)"
      - "tests/flux/test_phase1_robustness.py::test_no_T_test_regresses_under_extension PASSES (NEW; with R-1b extension active, each T-test passes on at least as many seeds as it did without R-1b — no regression)"
    time_budget_hours: 8
    status: None
    attempts: 1
    last_session: "2026-05-15T15:07:55.519505"
    blockers: ["R-1c must reach status=passed first — final gate before F2 work resumes"]
    created: 2026-05-14

  - id: R-1c-tris
    title: flux F1 — Bénard architecture iter-2 (smoothing + density-boost against Poisson noise)
    brief: docs/superpowers/plans/2026-05-15-flux-F1-robustness-iter2.md
    preregistered_acceptance:
      - "tests/flux/test_benard_robustness.py::test_T2_passes_on_at_least_8_of_10_seeds PASSES (same 10-seed grid as R-1c, threshold ≥8/10, no retuning)"
      - "tests/flux/test_benard_robustness.py::test_T2_FFT_SNR_above_3 PASSES (mean SNR ≥3.0 across passing seeds — R-1c saw NULL on 0/10)"
      - "tests/flux/test_benard_robustness.py::test_T2_negative_control_fails_all_10_seeds PASSES (buoyancy_g=0 must FAIL ALL 10 — R-1c had 2/10 spurious passes)"
      - "tests/flux/test_benard.py PASSES (seed=42 original test; session must remove @pytest.mark.slow marker added 2026-05-15)"
      - "tests/flux/test_horizontal_dynamics.py PASSES (R-1b pressure-grad tests or successors)"
      - "tests/flux/test_conservation.py PASSES (T1 holds)"
      - "tests/flux/test_crystallization.py PASSES (T3 unchanged by this item)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
    time_budget_hours: 12
    status: None
    attempts: 1
    last_session: "2026-05-15T18:13:54.745994"
    blockers: ["R-1c-bis NULL — third Bénard architecture variant (smoothing + density-boost against Poisson noise), per user mandate 2026-05-15 15:50"]
    created: 2026-05-15

  - id: R-1d-T3
    title: flux F1 — Crystallisation (T3) robustness audit + fix
    brief: docs/superpowers/plans/2026-05-15-flux-F1-robustness-iter2.md
    preregistered_acceptance:
      - "tests/flux/test_crystallization_robustness.py::test_T3_passes_on_at_least_8_of_10_seeds PASSES (NEW: parametrise across 10 seeds, threshold ≥8/10)"
      - "tests/flux/test_crystallization_robustness.py::test_T3_negative_control_fails_all_10_seeds PASSES (NEW: matched-wallclock no-binding control must FAIL ALL 10)"
      - "tests/flux/test_crystallization.py PASSES (seed=42 original test still passes)"
      - "tests/flux/test_conservation.py PASSES (T1 unaffected)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
    time_budget_hours: 12
    status: None
    attempts: 1
    last_session: "2026-05-15T20:57:08.197594"
    blockers: ["R-1d found T3 is 2/10 lucky — independent of T2 Bénard work; addresses the second fragility class"]
    created: 2026-05-15

  - id: R-1c-quad
    title: flux F1 — Bénard falsifier swap (FFT-of-T → vorticity field)
    brief: docs/superpowers/plans/2026-05-15-flux-F1-robustness-iter3.md
    preregistered_acceptance:
      - "tests/flux/test_benard_vorticity.py::test_T2_vorticity_passes_on_at_least_8_of_10_seeds PASSES (NEW: replaces FFT-of-T metric with vorticity-field FFT; same 10-seed grid; ≥8/10 threshold)"
      - "tests/flux/test_benard_vorticity.py::test_T2_vorticity_negative_control_fails_all_10_seeds PASSES (NEW: buoyancy_g=0 must FAIL 10/10 on the new metric)"
      - "tests/flux/test_conservation.py PASSES (T1 unaffected)"
      - "tests/flux/test_crystallization.py PASSES (T3 unaffected by metric change)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
      - "tests/flux/test_horizontal_dynamics.py PASSES (R-1b horizontal-dynamics tests still pass)"
    time_budget_hours: 12
    status: None
    attempts: 1
    last_session: "2026-05-16T03:40:11.342039"
    blockers: ["Five NULLs in iter-1/iter-2; vorticity is the classical mechanical signature of convection, not a retune"]
    created: 2026-05-15

  - id: R-1d-T3-bis
    title: flux F1 — Crystallisation (T3) iter-2 (lift bridge-flux starvation)
    brief: docs/superpowers/plans/2026-05-15-flux-F1-robustness-iter3.md
    preregistered_acceptance:
      - "tests/flux/test_crystallization_robustness.py::test_T3_passes_on_at_least_8_of_10_seeds PASSES (R-1d-T3 reached 6/10 with trimodal z-regime diagnosis; this iter must reach ≥8/10 by lifting bridge-flux starvation, preferably architectural not parametric)"
      - "tests/flux/test_crystallization_robustness.py::test_T3_negative_control_fails_all_10_seeds PASSES (no-binding control must FAIL 10/10)"
      - "tests/flux/test_crystallization.py PASSES (seed=42 original)"
      - "tests/flux/test_conservation.py PASSES (T1 unaffected)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
      - "tests/flux/test_bridges.py PASSES (F1b bridge layer must not regress)"
      - "tests/flux/test_plasticity.py PASSES (F1b plasticity must not regress)"
    time_budget_hours: 12
    status: passed
    attempts: 1
    last_session: "2026-05-16T09:56:42"
    blockers: ["R-1d-T3 phase-log specifically suggests this fix (lift bridge-flux starvation) as the next targeted iteration"]
    created: 2026-05-15

  - id: R-2
    title: flux F2 plan — cochlea + synthesis + first audio input
    brief: docs/superpowers/specs/2026-05-10-flux-substrate-design.md
    preregistered_acceptance:
      - "tests/test_research_plan_structure.py::test_F2_plan_exists_and_well_formed PASSES (verifies file at docs/superpowers/plans/<date>-flux-substrate-F2.md exists with required headings: Acceptance contract, File structure (locked decisions), Open calibration choices, Task N sections, at least one tests/flux/test_*.py acceptance target, reference to spec section 5)"
      - "Plan must declare these specific pytest acceptance paths so R-3 can verify them: tests/flux/test_cochlea.py and tests/flux/test_synthesis.py (this is the contract between R-2 and R-3 — R-2 picks the design, R-3 implements against it)"
      - "Plan must be compatible with autonomous-prototype-build skill (checkbox tasks, no free-form prose-only implementation)"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-14T08:08:22"
    blockers: ["R-1 must reach status=passed first"]
    created: 2026-05-13

  - id: R-3
    title: flux F2 — cochlea + synthesis implementation
    brief: docs/superpowers/plans/2026-05-14-flux-substrate-F2.md
    preregistered_acceptance:
      - "tests/flux/test_cochlea.py PASSES (pre-registered by R-2 as F2's cochlea acceptance test)"
      - "tests/flux/test_synthesis.py PASSES (pre-registered by R-2 as F2's synthesis acceptance test)"
      - "tests/flux/test_conservation.py PASSES (T1 conservation still holds with new cochlea + synthesis layers)"
      - "tests/flux/test_benard.py PASSES (T2 from R-1 still holds — F2 cochlea must not break thermal layer)"
      - "tests/flux/test_crystallization.py PASSES (T3 still holds)"
      - "tests/flux/test_decay.py PASSES (T4 still holds)"
    time_budget_hours: 16
    status: passed
    attempts: 2
    last_session: "2026-05-16T20:04:00"
    blockers: ["R-2 must reach status=passed first"]
    created: 2026-05-13

  - id: R-4
    title: flux F3 plan — learning-as-flux-reconfiguration
    brief: docs/superpowers/specs/2026-05-10-flux-substrate-design.md
    preregistered_acceptance:
      - "tests/test_research_plan_structure.py::test_F3_plan_exists_and_well_formed PASSES (verifies file at docs/superpowers/plans/<date>-flux-substrate-F3.md exists with required headings AND references the flux principle AND pre-registers a negative control)"
      - "Plan must declare these specific pytest acceptance paths so R-5 can verify them: tests/flux/test_learning.py and tests/flux/test_learning_negative_control.py (R-4 picks the learning rule, R-5 implements + runs falsification)"
      - "Plan defines a learning rule derived from the flux principle (NOT an STDP/BTSP/Hebbian re-implementation by another name)"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-16T22:46:25.567773"
    blockers: ["R-3 must reach status=passed first"]
    created: 2026-05-13

  - id: R-5
    title: flux F3 — learning implementation + falsification run
    brief: docs/superpowers/plans/2026-05-16-flux-substrate-F3.md
    preregistered_acceptance:
      - "tests/flux/test_learning.py PASSES (pre-registered by R-4 as F3 learning emergence acceptance — substrate exposed to a repeated input pattern develops measurable pattern-specific structure)"
      - "tests/flux/test_learning_negative_control.py PASSES (pre-registered by R-4 as F3 negative control — the same test on a matched-wallclock substrate WITHOUT input must show the substrate does NOT develop pattern-specific structure; verdict logic per F3 plan threshold)"
      - "tests/flux/test_conservation.py PASSES (T1 still holds with learning layer active)"
      - "tests/flux/test_benard.py PASSES (T2 still holds)"
      - "tests/flux/test_crystallization.py PASSES (T3 still holds)"
      - "tests/flux/test_decay.py PASSES (T4 still holds)"
    time_budget_hours: 24
    status: None
    attempts: 1
    last_session: "2026-05-17T02:58:29"
    blockers: ["R-4 must reach status=passed first"]
    created: 2026-05-13

  # ----- Training phase: only meaningful if R-5 (F3 learning) actually passed.
  # If F3 NULLed, these items will themselves NULL (their pytest targets depend
  # on the learning layer being live). That is the intended fail-shut behavior.
  # User instruction (2026-05-13 23:18): once the substrate works, also train it.

  - id: R-6
    title: training plan — expose flux substrate to a real English audio corpus
    brief: docs/superpowers/specs/2026-05-10-flux-substrate-design.md
    preregistered_acceptance:
      - "tests/test_research_plan_structure.py::test_training_EN_plan_exists_and_well_formed PASSES (verifies file at docs/superpowers/plans/<date>-flux-training-EN.md exists with: Acceptance contract, File structure (locked decisions), Open calibration choices, Task N sections, at least one tests/flux/test_*.py acceptance target, AND explicit pre-registered negative control + corpus specification)"
      - "Plan must declare these specific pytest acceptance paths so R-8 can verify them: tests/flux/test_training_run.py and tests/flux/test_training_negative_control.py"
      - "Plan body must specify: a single-language English corpus (no German, no French — per user 2026-05-13), Stage 1 (single-narrator audiobook) and Stage 2 (single YouTuber) and a Stage 4 SUBSTITUTE — Stage 3 multi-speaker remains out of vacation scope"
      - "Plan body must specify the Stage 4 substitute: >= 30 min single-speaker English recording from a public-domain or CC-licensed source, distinct speaker from Stage 1 and Stage 2 narrators (user delegated substitution 2026-05-13 23:38 — user on vacation, cannot record own voice). Plan must justify the choice with one paragraph on the acoustic or distributional difference between the Stage 4 substitute and Stages 1-2 (otherwise the substrate is just hearing 3 audiobook narrators with no real variation)" 
      - "Plan body must reference the F3 learning rule produced by R-5 by name and explain how the corpus exercises it"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-17T12:24:37.164875"
    blockers: ["R-5 must reach status=passed first"]
    created: 2026-05-13

  - id: R-7
    title: training corpus build — fetch English Stage 1+2 sources, validate, manifest
    brief: docs/superpowers/specs/2026-05-10-flux-substrate-design.md
    preregistered_acceptance:
      - "tests/flux/test_training_corpus_valid.py PASSES (corpus YAML at corpus.training-EN.yaml exists, parses, all listed audio assets exist on disk, total Stage 1 duration >= 60 min, total Stage 2 duration >= 120 min, sample-rate normalised to 16 kHz mono)"
      - "tests/flux/test_training_corpus_manifest.py PASSES (manifest written at ~/.eqmod/training/EN/manifest.json with: per-stage durations, source URLs, sha256 of each file)"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-17T13:08:56.999679"
    blockers: ["R-6 must reach status=passed first"]
    created: 2026-05-13

  - id: R-8
    title: training run — expose substrate to English corpus, measure emergence vs negative control
    brief: docs/superpowers/specs/2026-05-10-flux-substrate-design.md
    preregistered_acceptance:
      - "tests/flux/test_training_run.py PASSES (pre-registered by R-6 — substrate exposed to the English corpus develops the metric defined in the F3 + training plans above its threshold)"
      - "tests/flux/test_training_negative_control.py PASSES (pre-registered by R-6 — matched-wallclock substrate WITHOUT the English corpus must NOT develop the metric above threshold; verdict is PASS only when trained > control by the plan-specified margin)"
      - "docs/flux/phase-log.md has a training-run-close entry with measured numbers (substrate metric, control metric, margin, plan threshold) committed"
      - "LOGBOOK.md has the verdict + interpretation paragraph appended"
    time_budget_hours: 24
    status: None
    attempts: 1
    last_session: "2026-05-17T15:20:06.537245"
    blockers: ["R-7 must reach status=passed first"]
    created: 2026-05-13
  - id: R-9
    title: encoder-free audio — detailed plan (novel-path branch parallel to R-5..R-8 baseline)
    brief: docs/superpowers/plans/2026-05-16-flux-encoder-free-audio.md
    preregistered_acceptance:
      - "tests/test_research_plan_structure.py::test_encoder_free_plan_exists_and_well_formed PASSES (NEW meta-test the session must add: verifies detailed plan exists with required headings, declares R-10 and R-11 pytest paths, commits to specific energy-mapping rule + no-frequency-information constraint)"
      - "Plan body explicitly commits to: SR=16000, freq=constant=log(SR/2), 1 quantum per sample, energy=abs(sample) OR sample² (one choice, locked)"
      - "Plan declares pytest acceptance paths: tests/flux/test_audio_raw_injection.py for R-10; tests/flux/test_encoder_free_training_run.py + tests/flux/test_encoder_free_negative_control.py for R-11"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-17T16:02:22.492248"
    blockers: ["Novel-path: only meaningful if R-5 (F3 learning rule) actually works on the cochlea-baseline first. Sequenced after R-8 for that reason."]
    created: 2026-05-16

  - id: R-10
    title: encoder-free audio — implementation (raw-sample injection, no cochlea)
    brief: docs/superpowers/plans/2026-05-16-flux-encoder-free-audio.md
    preregistered_acceptance:
      - "tests/flux/test_audio_raw_injection.py::test_raw_injection_one_quantum_per_sample PASSES (NEW: 0.1 s 1 kHz sine at 16 kHz SR → exactly 1600 quanta, all freq=log(SR/2), energies track rectified waveform)"
      - "tests/flux/test_audio_raw_injection.py::test_raw_injection_silence_injects_zero_energy PASSES (NEW: 0.1 s silence → injected quanta have energy ≈ 0 within float epsilon)"
      - "tests/flux/test_audio_raw_injection.py::test_raw_injection_amplitude_modulation_visible_in_substrate PASSES (NEW: AM tone → Pearson r ≥ 0.7 between input envelope and substrate floor-energy time-series)"
      - "tests/flux/test_conservation.py PASSES (T1 unaffected)"
      - "tests/flux/test_cochlea.py PASSES (F2 baseline cochlea must NOT be modified — separate code path)"
      - "tests/flux/test_synthesis.py PASSES (F2 baseline synthesis unchanged)"
    time_budget_hours: 8
    status: passed
    attempts: 1
    last_session: "2026-05-17T16:43:48.920099"
    blockers: ["R-9 must reach status=passed first"]
    created: 2026-05-16

  - id: R-11
    title: encoder-free audio — training run + falsification + R-8 baseline comparison
    brief: docs/superpowers/plans/2026-05-16-flux-encoder-free-audio.md
    preregistered_acceptance:
      - "tests/flux/test_encoder_free_training_run.py::test_encoder_free_substrate_distinguishable_from_no_input_control PASSES (NEW: KL divergence of encoder-free babble-MFCC-histogram vs no-input control histogram > 2σ on 100-bootstrap — substrate must have learned SOMETHING from raw audio)"
      - "tests/flux/test_encoder_free_negative_control.py::test_no_input_control_produces_no_substrate_specific_signal PASSES (NEW: no-input substrate babble statistically indistinguishable from white-noise MFCC distribution — sanity check)"
      - "docs/flux/phase-log.md has R-11 entry with both runs bootstrap means + stds + verdict comparison"
      - "LOGBOOK.md has cross-comparison observation: tabulate KL divergences for R-8 cochlea-baseline vs R-11 encoder-free; measure the gap and report it (PASS verdict does NOT require beating R-8 — only measuring the gap)"
    time_budget_hours: 24
    status: None
    attempts: 1
    last_session: "2026-05-17T17:54:07.241520"
    blockers: ["R-10 must reach status=passed first; also R-8 baseline ideally completed for comparison data"]
    created: 2026-05-16
  - id: R-12
    title: spec amendment §5.8 — activation field overlay implementation
    brief: docs/superpowers/specs/2026-05-17-flux-spec-amendment-activation-field.md
    preregistered_acceptance:
      - "tests/flux/test_activation_field.py::test_field_decays_exponentially PASSES (NEW: 0.69 s half-life within 1% tolerance)"
      - "tests/flux/test_activation_field.py::test_field_responds_to_firings PASSES (NEW: steady-state magnitude tracks N*beta/alpha)"
      - "tests/flux/test_activation_field.py::test_coincidence_zero_when_both_endpoints_silent PASSES (NEW: A=0 → coincidence=0)"
      - "tests/flux/test_activation_field.py::test_coincidence_increases_with_paired_activity PASSES (NEW: monotone in paired A)"
      - "tests/flux/test_conservation.py PASSES (T1 holds — field is bookkeeping, no energy reservoir)"
      - "tests/flux/test_crystallization_robustness.py PASSES (T3 9/10 from R-1d-T3-bis stays robust)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
      - "tests/flux/test_learning.py PASSES (R-5 fast tests stay green; slow excluded)"
    time_budget_hours: 4
    status: None
    attempts: 1
    last_session: "2026-05-18T04:44:56.880665"
    blockers: ["Spec amendment §5.8 by Claude under user authorization 2026-05-17 23:00 — adds continuous activation field A(x,y,z,t) as plasticity-signal smoother. Discrete substrate primary; field is bookkeeping."]
    created: 2026-05-17
  - id: R-13
    title: bridge-weight spectrum — substrate-internal observable bypassing synthesis
    brief: docs/superpowers/plans/2026-05-19-flux-encoder-free-iter2.md
    preregistered_acceptance:
      - "tests/flux/test_bridge_spectrum.py::test_bridge_spectrum_observable_constructs PASSES (NEW: extract (freq, weight) pairs across all alive bridges, normalised histogram >= 32 bins populated)"
      - "tests/flux/test_bridge_spectrum.py::test_bridge_spectrum_zero_on_empty_substrate PASSES (NEW: 0-node substrate returns empty distribution, not NaN/error)"
      - "tests/flux/test_bridge_spectrum.py::test_bridge_spectrum_differs_under_different_audio PASSES (NEW: 50k-tick runs on English vs matched-RMS white noise must give KL > 0.1 between spectra)"
      - "tests/flux/test_conservation.py PASSES (T1 robust)"
      - "tests/flux/test_crystallization_robustness.py PASSES (T3 robust)"
    time_budget_hours: 4
    status: failed
    attempts: 3
    last_session: "2026-05-19T10:16:51"
    blockers: ["R-LR-1 diagnosed synthesis as the bottleneck; this item bypasses synthesis entirely with a substrate-internal observable", "main-sync force-set 2026-05-19: autopilot/R-13 branch reached 3-strike NULL terminal at attempt 3 (commit c4edbb2), then 6 charter-refused follow-on sessions (04c332f..831b548) failed to sync verdict to main. Manual force-set per branch state."]
    created: 2026-05-19

  - id: R-14
    title: synthesis sensitivity sweep — find Q/gain where firings dominate baseline
    brief: docs/superpowers/plans/2026-05-19-flux-encoder-free-iter2.md
    preregistered_acceptance:
      - "tests/flux/test_synthesis_sensitivity.py::test_synthesis_baseline_with_empty_substrate_has_low_energy PASSES (NEW: empty-substrate synthesis RMS < 0.1 * trained-substrate synthesis RMS)"
      - "tests/flux/test_synthesis_sensitivity.py::test_synthesis_with_firings_dominates_baseline PASSES (NEW: 100 firings/sec drive produces FFT peak at firing-pattern freq, not resonator natural freq)"
      - "Session picks smallest (Q, gain) from sweep [Q=3,5,10,30] x [gain=1,5,25,100] that passes both. Documents choice in phase-log"
      - "tests/flux/test_synthesis.py PASSES (F2 baseline synthesis unit tests still pass at original Q=10 gain=1 defaults; new combo is additional config)"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-19T15:45:08.594028"
    blockers: ["R-LR-1 diagnosed synthesis-baseline as the dominant signal in babble; this item tunes Q/gain so firings can break through"]
    created: 2026-05-19

  - id: R-15
    title: R-LR-8 infrastructure — salvage R-13 spectrum + add flux snapshot + wire tuned synthesis env
    brief: docs/superpowers/plans/2026-05-19-flux-encoder-free-iter2.md
    preregistered_acceptance:
      - "tests/flux/test_flux_snapshot.py::test_roundtrip_preserves_substrate_state PASSES (NEW: save flux Quanta+Nodes+Bridges+Grid+tick state to .npz, load back, verify all arrays bit-identical and substrate dynamics reproduce from loaded state)"
      - "tests/flux/test_flux_snapshot.py::test_snapshot_emits_at_configured_interval PASSES (NEW: with EQMOD_SNAPSHOT_EVERY_TICKS=100, encoder_free_training runner writes exactly 5 snapshot files after 500 ticks at the expected tick counts {100, 200, 300, 400, 500})"
      - "tests/flux/test_bridge_spectrum.py::test_bridge_spectrum_observable_constructs PASSES (salvaged from autopilot/R-13 commit eca9545 — R-13 NULLed on architectural firewall not function; the function itself passed at 50k-tick scope; session brings agent/flux/bridge_spectrum.py + corpus_spectrum.py + test file to main via git checkout autopilot/R-13 -- <paths>)"
      - "tests/flux/test_bridge_spectrum.py::test_bridge_spectrum_zero_on_empty_substrate PASSES (same salvage)"
      - "tests/flux/test_tuned_synthesis_env.py::test_env_loads_Q3_gain1 PASSES (NEW: with EQMOD_USE_TUNED_SYNTHESIS=1, agent.flux.synthesis.get_synthesis_config returns SynthesisConfig with Q=3.0 and impulse_gain=1.0, per R-14's locked sweep choice in commit e9c9275)"
      - "tests/flux/test_R_LR_8_acceptance.py is collected by pytest (NEW: file imports cleanly, defines test_bridge_spectrum_audio_vs_white_noise and test_synthesis_audio_vs_no_input with the pre-registered thresholds KL>0.5 and KL>0.001 from docs/superpowers/plans/2026-05-19-flux-encoder-free-iter2.md; tests skip gracefully when EQMOD_R_LR_8_OUT_DIR is unset, so the file collects cleanly under pytest before R-LR-8 has run; the file does NOT pass without R-LR-8's output — it scaffolds the long-run gate)"
      - "tests/flux/test_conservation.py PASSES (T1 robust under new snapshot code)"
      - "pytest -m 'not slow' PASSES (no regressions in the legacy or flux fast-test slice)"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-20T00:20:27.688452"
    blockers: ["R-14 passed (locked Q=3 gain=1 in commit e9c9275); session may reference R-14 phase-log for synthesis env conventions"]
    created: 2026-05-19

  - id: R-16
    title: architectural firewall diagnostic — 50k smoke for content-coupling in nodes.energy
    brief: docs/superpowers/plans/2026-05-19-flux-encoder-free-iter2.md
    preregistered_acceptance:
      - "tests/flux/test_firewall_diagnostic.py::test_50k_matched_rms_english_vs_white_noise_bridge_spectrum_KL_above_0p01 PASSES (NEW: run two encoder-free 50k-tick substrates with identical seed and identical RMS amplitude — one fed English audio segment from agent/flux/audio fixtures, one fed white noise of matched RMS — compute bridge spectra on the alive bridges of each, assert symmetric KL > 0.01. Threshold 0.01 chosen pre-data as the smallest non-trivial value above floating-point noise floor at 50k-tick scope.)"
      - "tests/flux/test_firewall_diagnostic.py::test_50k_matched_rms_english_vs_silence_bridge_spectrum_KL_above_0p1 PASSES (NEW: control test — English audio vs silence with same RMS-energy budget injected as noise floor; KL must exceed 0.1, because silence is the maximally different input. If THIS test also nulls, the firewall is total, not partial.)"
      - "LOGBOOK entry records both KL numbers and the audio fixtures used. If KL_english_vs_whitenoise < 0.01, the entry must explicitly name this as confirming R-13's architectural-firewall forensic finding (eca9545) and motivate the G24 amendment as the next item."
      - "tests/flux/test_conservation.py PASSES (T1 robust under whatever fixtures the session uses)"
      - "tests/flux/test_crystallization_robustness.py PASSES (T3 robust)"
    time_budget_hours: 4
    status: null
    attempts: 1
    last_session: "2026-05-20T01:33:17.764176"
    blockers: ["R-15 must reach status=passed first (provides agent/flux/bridge_spectrum.py + corpus_spectrum.py)"]
    created: 2026-05-19

