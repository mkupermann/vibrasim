# EQMOD Research Queue — Vacation 2026-05-14 .. 2026-05-28
#
# Multi-attempt flux-substrate emergence sprint. Replaces the prior
# Maintenance queue (preserved at QUEUE.maintenance.yaml.bak — AUTO-2 and
# AUTO-3 stay queued there for post-vacation pickup).
#
# Mission: bottom-up emergence of binding from a single principle
# (energy flux), not from engineered binding rules. Each item advances
# the flux substrate by one pre-registered stage. NULL is a valid verdict.
#
# Briefs point at existing plan documents under docs/superpowers/plans/.
# Acceptance is locked at in_progress (pre-commit hook blocks edits to
# the preregistered_acceptance: block).
#
# Status-Übergänge sind Postflight-Maschinenvorgang — Claude darf Status
# nicht direkt editieren (pre-commit hook blockt).
#
# Status-Werte:
#   queued       - noch nicht angerührt
#   in_progress  - aktuelle Session arbeitet daran (1 Item gleichzeitig)
#   passed       - acceptance erfüllt
#   null         - acceptance nicht erfüllt nach fair attempt, postmortem in LOGBOOK
#   failed       - 3 attempts ohne pass, written-off
#   blocked      - HUMAN_NEEDED, wartet auf Michael

window:
  start: 2026-05-14
  end:   2026-05-28
  default_model: claude-opus-4-7
  per_session_max_turns: 80
  per_session_wallclock_hours: 4

items:
  - id: R-1
    title: flux F1c — thermal dynamics + T2 Bénard convection
    brief: docs/superpowers/plans/2026-05-11-flux-substrate-F1c.md
    preregistered_acceptance:
      - "tests/flux/test_thermal.py PASSES (6 new unit tests per plan Task 2)"
      - "tests/flux/test_benard.py::test_T2_benard_horizontal_wavelength PASSES (T2 acceptance per plan Task 5)"
      - "tests/flux/test_conservation.py PASSES (T1 holds with buoyancy + damping active)"
      - "tests/flux/test_crystallization.py PASSES (T3 unaffected by thermal layer)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected by thermal layer)"
      - "pytest -m 'not slow' PASSES (legacy regression baseline holds)"
    time_budget_hours: 12
    status: passed
    attempts: 1
    last_session: "2026-05-14T00:11:39"
    blockers: []
    created: 2026-05-13

  - id: R-1b
    title: flux F1 — architectural extension (horizontal force / pressure surrogate)
    brief: docs/superpowers/plans/2026-05-14-flux-F1-robustness.md
    preregistered_acceptance:
      - "tests/flux/test_horizontal_dynamics.py::test_horizontal_force_responds_to_T_gradient PASSES (NEW test the session writes; >=80% of alive quanta acquire non-zero horizontal velocity under a horizontal T gradient)"
      - "tests/flux/test_horizontal_dynamics.py::test_horizontal_force_zero_when_no_gradient PASSES (NEW; mean horizontal velocity < 0.01 under uniform T — confirms force fires only on real gradients)"
      - "tests/flux/test_conservation.py PASSES (T1 still holds with new force active)"
      - "tests/flux/test_crystallization.py PASSES (T3 unaffected)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
      - "tests/flux/test_thermal.py PASSES (existing thermal unit tests unaffected)"
      - "tests/flux/test_dynamics.py PASSES (dynamics smoke unaffected)"
    time_budget_hours: 12
    status: passed
    attempts: 2
    last_session: "2026-05-14T14:13:13"
    blockers: ["R-1 passed (F1c baseline imported to main); architectural fix replaces lucky-seed Bénard pass"]
    created: 2026-05-14

  - id: R-1c
    title: flux F1 — Bénard multi-seed robustness audit
    brief: docs/superpowers/plans/2026-05-14-flux-F1-robustness.md
    preregistered_acceptance:
      - "tests/flux/test_benard_robustness.py::test_T2_passes_on_at_least_8_of_10_seeds PASSES (NEW; parametrise across seeds 7,13,21,42,100,137,256,314,500,1000; at least 8/10 must satisfy the wavelength check)"
      - "tests/flux/test_benard_robustness.py::test_T2_FFT_SNR_above_3 PASSES (NEW; mean FFT signal-to-noise ratio across passing seeds must be >=3.0 — was 1.5 in original F1c)"
      - "tests/flux/test_benard_robustness.py::test_T2_negative_control_fails_all_10_seeds PASSES (NEW; with buoyancy_g=0, ALL 10 seeds must FAIL the wavelength check — substrate must not produce horizontal structure from RNG alone)"
      - "tests/flux/test_benard.py PASSES (original seed=42 acceptance unchanged)"
    time_budget_hours: 12
    status: failed
    attempts: 1
    last_session: "2026-05-15T08:32:49.373331"
    blockers: ["R-1b must reach status=passed first", "Bénard-multi-seed iterativ ersetzt durch R-1c-bis (Triage 2026-05-21)"]
    created: 2026-05-14

  - id: R-1c-bis
    title: flux F1 — Bénard architecture variant 2 (return-flow injector at cold ceiling)
    brief: docs/superpowers/plans/2026-05-14-flux-F1-robustness.md
    preregistered_acceptance:
      - "tests/flux/test_benard_robustness.py::test_T2_passes_on_at_least_8_of_10_seeds PASSES (same 10-seed grid as R-1c; threshold ≥8/10; no retuning)"
      - "tests/flux/test_benard_robustness.py::test_T2_FFT_SNR_above_3 PASSES (mean SNR ≥3.0 across passing seeds — R-1c saw NULL on 0/10 passes)"
      - "tests/flux/test_benard_robustness.py::test_T2_negative_control_fails_all_10_seeds PASSES (buoyancy_g=0 must FAIL on ALL 10 seeds — R-1c had 2/10 spurious passes here, this gate is the discriminating-metric check)"
      - "tests/flux/test_benard.py PASSES (original seed=42 acceptance; session must remove the @pytest.mark.slow marker that was added 2026-05-15)"
      - "tests/flux/test_horizontal_dynamics.py PASSES (R-1b's pressure-gradient tests; may stay or be modified per the session's architectural decision, but cannot regress)"
      - "tests/flux/test_conservation.py PASSES (T1 holds)"
      - "tests/flux/test_crystallization.py PASSES (T3 unaffected)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
    time_budget_hours: 16
    status: failed
    attempts: 1
    last_session: "2026-05-15T12:01:59.081816"
    blockers: ["R-1c NULL — adaptive iteration per user mandate 2026-05-15 10:25", "Ersetzt durch R-1c-tris (Triage 2026-05-21)"]
    created: 2026-05-15

  - id: R-1d
    title: flux F1 — Phase-1 joint robustness gate
    brief: docs/superpowers/plans/2026-05-14-flux-F1-robustness.md
    preregistered_acceptance:
      - "tests/flux/test_phase1_robustness.py::test_all_T_tests_pass_jointly_on_8_of_10_seeds PASSES (NEW; T1+T2+T3+T4 all pass on the same seed for >=8 of the 10 seeds; identical parameter envelope across seeds, no per-seed tweaks)"
      - "tests/flux/test_phase1_robustness.py::test_no_T_test_regresses_under_extension PASSES (NEW; with R-1b extension active, each T-test passes on at least as many seeds as it did without R-1b — no regression)"
    time_budget_hours: 8
    status: failed
    attempts: 1
    last_session: "2026-05-15T15:07:55.519505"
    blockers: ["R-1c must reach status=passed first — final gate before F2 work resumes", "Phase-1 joint robustness — abhaengig von R-1c das tot ist (Triage 2026-05-21)"]
    created: 2026-05-14

  - id: R-1c-tris
    title: flux F1 — Bénard architecture iter-2 (smoothing + density-boost against Poisson noise)
    brief: docs/superpowers/plans/2026-05-15-flux-F1-robustness-iter2.md
    preregistered_acceptance:
      - "tests/flux/test_benard_robustness.py::test_T2_passes_on_at_least_8_of_10_seeds PASSES (same 10-seed grid as R-1c, threshold ≥8/10, no retuning)"
      - "tests/flux/test_benard_robustness.py::test_T2_FFT_SNR_above_3 PASSES (mean SNR ≥3.0 across passing seeds — R-1c saw NULL on 0/10)"
      - "tests/flux/test_benard_robustness.py::test_T2_negative_control_fails_all_10_seeds PASSES (buoyancy_g=0 must FAIL ALL 10 — R-1c had 2/10 spurious passes)"
      - "tests/flux/test_benard.py PASSES (seed=42 original test; session must remove @pytest.mark.slow marker added 2026-05-15)"
      - "tests/flux/test_horizontal_dynamics.py PASSES (R-1b pressure-grad tests or successors)"
      - "tests/flux/test_conservation.py PASSES (T1 holds)"
      - "tests/flux/test_crystallization.py PASSES (T3 unchanged by this item)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
    time_budget_hours: 12
    status: failed
    attempts: 1
    last_session: "2026-05-15T18:13:54.745994"
    blockers: ["R-1c-bis NULL — third Bénard architecture variant (smoothing + density-boost against Poisson noise), per user mandate 2026-05-15 15:50", "Bénard-Pfad nicht weiterverfolgt (Triage 2026-05-21)"]
    created: 2026-05-15

  - id: R-1d-T3
    title: flux F1 — Crystallisation (T3) robustness audit + fix
    brief: docs/superpowers/plans/2026-05-15-flux-F1-robustness-iter2.md
    preregistered_acceptance:
      - "tests/flux/test_crystallization_robustness.py::test_T3_passes_on_at_least_8_of_10_seeds PASSES (NEW: parametrise across 10 seeds, threshold ≥8/10)"
      - "tests/flux/test_crystallization_robustness.py::test_T3_negative_control_fails_all_10_seeds PASSES (NEW: matched-wallclock no-binding control must FAIL ALL 10)"
      - "tests/flux/test_crystallization.py PASSES (seed=42 original test still passes)"
      - "tests/flux/test_conservation.py PASSES (T1 unaffected)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
    time_budget_hours: 12
    status: None
    attempts: 1
    last_session: "2026-05-15T20:57:08.197594"
    blockers: ["R-1d found T3 is 2/10 lucky — independent of T2 Bénard work; addresses the second fragility class"]
    created: 2026-05-15

  - id: R-1c-quad
    title: flux F1 — Bénard falsifier swap (FFT-of-T → vorticity field)
    brief: docs/superpowers/plans/2026-05-15-flux-F1-robustness-iter3.md
    preregistered_acceptance:
      - "tests/flux/test_benard_vorticity.py::test_T2_vorticity_passes_on_at_least_8_of_10_seeds PASSES (NEW: replaces FFT-of-T metric with vorticity-field FFT; same 10-seed grid; ≥8/10 threshold)"
      - "tests/flux/test_benard_vorticity.py::test_T2_vorticity_negative_control_fails_all_10_seeds PASSES (NEW: buoyancy_g=0 must FAIL 10/10 on the new metric)"
      - "tests/flux/test_conservation.py PASSES (T1 unaffected)"
      - "tests/flux/test_crystallization.py PASSES (T3 unaffected by metric change)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
      - "tests/flux/test_horizontal_dynamics.py PASSES (R-1b horizontal-dynamics tests still pass)"
    time_budget_hours: 12
    status: None
    attempts: 1
    last_session: "2026-05-16T03:40:11.342039"
    blockers: ["Five NULLs in iter-1/iter-2; vorticity is the classical mechanical signature of convection, not a retune"]
    created: 2026-05-15

  - id: R-1d-T3-bis
    title: flux F1 — Crystallisation (T3) iter-2 (lift bridge-flux starvation)
    brief: docs/superpowers/plans/2026-05-15-flux-F1-robustness-iter3.md
    preregistered_acceptance:
      - "tests/flux/test_crystallization_robustness.py::test_T3_passes_on_at_least_8_of_10_seeds PASSES (R-1d-T3 reached 6/10 with trimodal z-regime diagnosis; this iter must reach ≥8/10 by lifting bridge-flux starvation, preferably architectural not parametric)"
      - "tests/flux/test_crystallization_robustness.py::test_T3_negative_control_fails_all_10_seeds PASSES (no-binding control must FAIL 10/10)"
      - "tests/flux/test_crystallization.py PASSES (seed=42 original)"
      - "tests/flux/test_conservation.py PASSES (T1 unaffected)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
      - "tests/flux/test_bridges.py PASSES (F1b bridge layer must not regress)"
      - "tests/flux/test_plasticity.py PASSES (F1b plasticity must not regress)"
    time_budget_hours: 12
    status: passed
    attempts: 1
    last_session: "2026-05-16T09:56:42"
    blockers: ["R-1d-T3 phase-log specifically suggests this fix (lift bridge-flux starvation) as the next targeted iteration"]
    created: 2026-05-15

  - id: R-2
    title: flux F2 plan — cochlea + synthesis + first audio input
    brief: docs/superpowers/specs/2026-05-10-flux-substrate-design.md
    preregistered_acceptance:
      - "tests/test_research_plan_structure.py::test_F2_plan_exists_and_well_formed PASSES (verifies file at docs/superpowers/plans/<date>-flux-substrate-F2.md exists with required headings: Acceptance contract, File structure (locked decisions), Open calibration choices, Task N sections, at least one tests/flux/test_*.py acceptance target, reference to spec section 5)"
      - "Plan must declare these specific pytest acceptance paths so R-3 can verify them: tests/flux/test_cochlea.py and tests/flux/test_synthesis.py (this is the contract between R-2 and R-3 — R-2 picks the design, R-3 implements against it)"
      - "Plan must be compatible with autonomous-prototype-build skill (checkbox tasks, no free-form prose-only implementation)"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-14T08:08:22"
    blockers: ["R-1 must reach status=passed first"]
    created: 2026-05-13

  - id: R-3
    title: flux F2 — cochlea + synthesis implementation
    brief: docs/superpowers/plans/2026-05-14-flux-substrate-F2.md
    preregistered_acceptance:
      - "tests/flux/test_cochlea.py PASSES (pre-registered by R-2 as F2's cochlea acceptance test)"
      - "tests/flux/test_synthesis.py PASSES (pre-registered by R-2 as F2's synthesis acceptance test)"
      - "tests/flux/test_conservation.py PASSES (T1 conservation still holds with new cochlea + synthesis layers)"
      - "tests/flux/test_benard.py PASSES (T2 from R-1 still holds — F2 cochlea must not break thermal layer)"
      - "tests/flux/test_crystallization.py PASSES (T3 still holds)"
      - "tests/flux/test_decay.py PASSES (T4 still holds)"
    time_budget_hours: 16
    status: passed
    attempts: 2
    last_session: "2026-05-16T20:04:00"
    blockers: ["R-2 must reach status=passed first"]
    created: 2026-05-13

  - id: R-4
    title: flux F3 plan — learning-as-flux-reconfiguration
    brief: docs/superpowers/specs/2026-05-10-flux-substrate-design.md
    preregistered_acceptance:
      - "tests/test_research_plan_structure.py::test_F3_plan_exists_and_well_formed PASSES (verifies file at docs/superpowers/plans/<date>-flux-substrate-F3.md exists with required headings AND references the flux principle AND pre-registers a negative control)"
      - "Plan must declare these specific pytest acceptance paths so R-5 can verify them: tests/flux/test_learning.py and tests/flux/test_learning_negative_control.py (R-4 picks the learning rule, R-5 implements + runs falsification)"
      - "Plan defines a learning rule derived from the flux principle (NOT an STDP/BTSP/Hebbian re-implementation by another name)"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-16T22:46:25.567773"
    blockers: ["R-3 must reach status=passed first"]
    created: 2026-05-13

  - id: R-5
    title: flux F3 — learning implementation + falsification run
    brief: docs/superpowers/plans/2026-05-16-flux-substrate-F3.md
    preregistered_acceptance:
      - "tests/flux/test_learning.py PASSES (pre-registered by R-4 as F3 learning emergence acceptance — substrate exposed to a repeated input pattern develops measurable pattern-specific structure)"
      - "tests/flux/test_learning_negative_control.py PASSES (pre-registered by R-4 as F3 negative control — the same test on a matched-wallclock substrate WITHOUT input must show the substrate does NOT develop pattern-specific structure; verdict logic per F3 plan threshold)"
      - "tests/flux/test_conservation.py PASSES (T1 still holds with learning layer active)"
      - "tests/flux/test_benard.py PASSES (T2 still holds)"
      - "tests/flux/test_crystallization.py PASSES (T3 still holds)"
      - "tests/flux/test_decay.py PASSES (T4 still holds)"
    time_budget_hours: 24
    status: None
    attempts: 1
    last_session: "2026-05-17T02:58:29"
    blockers: ["R-4 must reach status=passed first"]
    created: 2026-05-13

  # ----- Training phase: only meaningful if R-5 (F3 learning) actually passed.
  # If F3 NULLed, these items will themselves NULL (their pytest targets depend
  # on the learning layer being live). That is the intended fail-shut behavior.
  # User instruction (2026-05-13 23:18): once the substrate works, also train it.

  - id: R-6
    title: training plan — expose flux substrate to a real English audio corpus
    brief: docs/superpowers/specs/2026-05-10-flux-substrate-design.md
    preregistered_acceptance:
      - "tests/test_research_plan_structure.py::test_training_EN_plan_exists_and_well_formed PASSES (verifies file at docs/superpowers/plans/<date>-flux-training-EN.md exists with: Acceptance contract, File structure (locked decisions), Open calibration choices, Task N sections, at least one tests/flux/test_*.py acceptance target, AND explicit pre-registered negative control + corpus specification)"
      - "Plan must declare these specific pytest acceptance paths so R-8 can verify them: tests/flux/test_training_run.py and tests/flux/test_training_negative_control.py"
      - "Plan body must specify: a single-language English corpus (no German, no French — per user 2026-05-13), Stage 1 (single-narrator audiobook) and Stage 2 (single YouTuber) and a Stage 4 SUBSTITUTE — Stage 3 multi-speaker remains out of vacation scope"
      - "Plan body must specify the Stage 4 substitute: >= 30 min single-speaker English recording from a public-domain or CC-licensed source, distinct speaker from Stage 1 and Stage 2 narrators (user delegated substitution 2026-05-13 23:38 — user on vacation, cannot record own voice). Plan must justify the choice with one paragraph on the acoustic or distributional difference between the Stage 4 substitute and Stages 1-2 (otherwise the substrate is just hearing 3 audiobook narrators with no real variation)" 
      - "Plan body must reference the F3 learning rule produced by R-5 by name and explain how the corpus exercises it"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-17T12:24:37.164875"
    blockers: ["R-5 must reach status=passed first"]
    created: 2026-05-13

  - id: R-7
    title: training corpus build — fetch English Stage 1+2 sources, validate, manifest
    brief: docs/superpowers/specs/2026-05-10-flux-substrate-design.md
    preregistered_acceptance:
      - "tests/flux/test_training_corpus_valid.py PASSES (corpus YAML at corpus.training-EN.yaml exists, parses, all listed audio assets exist on disk, total Stage 1 duration >= 60 min, total Stage 2 duration >= 120 min, sample-rate normalised to 16 kHz mono)"
      - "tests/flux/test_training_corpus_manifest.py PASSES (manifest written at ~/.eqmod/training/EN/manifest.json with: per-stage durations, source URLs, sha256 of each file)"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-17T13:08:56.999679"
    blockers: ["R-6 must reach status=passed first"]
    created: 2026-05-13

  - id: R-8
    title: training run — expose substrate to English corpus, measure emergence vs negative control
    brief: docs/superpowers/specs/2026-05-10-flux-substrate-design.md
    preregistered_acceptance:
      - "tests/flux/test_training_run.py PASSES (pre-registered by R-6 — substrate exposed to the English corpus develops the metric defined in the F3 + training plans above its threshold)"
      - "tests/flux/test_training_negative_control.py PASSES (pre-registered by R-6 — matched-wallclock substrate WITHOUT the English corpus must NOT develop the metric above threshold; verdict is PASS only when trained > control by the plan-specified margin)"
      - "docs/flux/phase-log.md has a training-run-close entry with measured numbers (substrate metric, control metric, margin, plan threshold) committed"
      - "LOGBOOK.md has the verdict + interpretation paragraph appended"
    time_budget_hours: 24
    status: None
    attempts: 1
    last_session: "2026-05-17T15:20:06.537245"
    blockers: ["R-7 must reach status=passed first"]
    created: 2026-05-13
  - id: R-9
    title: encoder-free audio — detailed plan (novel-path branch parallel to R-5..R-8 baseline)
    brief: docs/superpowers/plans/2026-05-16-flux-encoder-free-audio.md
    preregistered_acceptance:
      - "tests/test_research_plan_structure.py::test_encoder_free_plan_exists_and_well_formed PASSES (NEW meta-test the session must add: verifies detailed plan exists with required headings, declares R-10 and R-11 pytest paths, commits to specific energy-mapping rule + no-frequency-information constraint)"
      - "Plan body explicitly commits to: SR=16000, freq=constant=log(SR/2), 1 quantum per sample, energy=abs(sample) OR sample² (one choice, locked)"
      - "Plan declares pytest acceptance paths: tests/flux/test_audio_raw_injection.py for R-10; tests/flux/test_encoder_free_training_run.py + tests/flux/test_encoder_free_negative_control.py for R-11"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-17T16:02:22.492248"
    blockers: ["Novel-path: only meaningful if R-5 (F3 learning rule) actually works on the cochlea-baseline first. Sequenced after R-8 for that reason."]
    created: 2026-05-16

  - id: R-10
    title: encoder-free audio — implementation (raw-sample injection, no cochlea)
    brief: docs/superpowers/plans/2026-05-16-flux-encoder-free-audio.md
    preregistered_acceptance:
      - "tests/flux/test_audio_raw_injection.py::test_raw_injection_one_quantum_per_sample PASSES (NEW: 0.1 s 1 kHz sine at 16 kHz SR → exactly 1600 quanta, all freq=log(SR/2), energies track rectified waveform)"
      - "tests/flux/test_audio_raw_injection.py::test_raw_injection_silence_injects_zero_energy PASSES (NEW: 0.1 s silence → injected quanta have energy ≈ 0 within float epsilon)"
      - "tests/flux/test_audio_raw_injection.py::test_raw_injection_amplitude_modulation_visible_in_substrate PASSES (NEW: AM tone → Pearson r ≥ 0.7 between input envelope and substrate floor-energy time-series)"
      - "tests/flux/test_conservation.py PASSES (T1 unaffected)"
      - "tests/flux/test_cochlea.py PASSES (F2 baseline cochlea must NOT be modified — separate code path)"
      - "tests/flux/test_synthesis.py PASSES (F2 baseline synthesis unchanged)"
    time_budget_hours: 8
    status: passed
    attempts: 1
    last_session: "2026-05-17T16:43:48.920099"
    blockers: ["R-9 must reach status=passed first"]
    created: 2026-05-16

  - id: R-11
    title: encoder-free audio — training run + falsification + R-8 baseline comparison
    brief: docs/superpowers/plans/2026-05-16-flux-encoder-free-audio.md
    preregistered_acceptance:
      - "tests/flux/test_encoder_free_training_run.py::test_encoder_free_substrate_distinguishable_from_no_input_control PASSES (NEW: KL divergence of encoder-free babble-MFCC-histogram vs no-input control histogram > 2σ on 100-bootstrap — substrate must have learned SOMETHING from raw audio)"
      - "tests/flux/test_encoder_free_negative_control.py::test_no_input_control_produces_no_substrate_specific_signal PASSES (NEW: no-input substrate babble statistically indistinguishable from white-noise MFCC distribution — sanity check)"
      - "docs/flux/phase-log.md has R-11 entry with both runs bootstrap means + stds + verdict comparison"
      - "LOGBOOK.md has cross-comparison observation: tabulate KL divergences for R-8 cochlea-baseline vs R-11 encoder-free; measure the gap and report it (PASS verdict does NOT require beating R-8 — only measuring the gap)"
    time_budget_hours: 24
    status: failed
    attempts: 1
    last_session: "2026-05-17T17:54:07.241520"
    blockers: ["R-10 must reach status=passed first; also R-8 baseline ideally completed for comparison data", "Ersetzt durch R-LR-1 (full-scale-Pendant), siehe LOGBOOK 2026-05-19 (Triage 2026-05-21)"]
    created: 2026-05-16
  - id: R-12
    title: spec amendment §5.8 — activation field overlay implementation
    brief: docs/superpowers/specs/2026-05-17-flux-spec-amendment-activation-field.md
    preregistered_acceptance:
      - "tests/flux/test_activation_field.py::test_field_decays_exponentially PASSES (NEW: 0.69 s half-life within 1% tolerance)"
      - "tests/flux/test_activation_field.py::test_field_responds_to_firings PASSES (NEW: steady-state magnitude tracks N*beta/alpha)"
      - "tests/flux/test_activation_field.py::test_coincidence_zero_when_both_endpoints_silent PASSES (NEW: A=0 → coincidence=0)"
      - "tests/flux/test_activation_field.py::test_coincidence_increases_with_paired_activity PASSES (NEW: monotone in paired A)"
      - "tests/flux/test_conservation.py PASSES (T1 holds — field is bookkeeping, no energy reservoir)"
      - "tests/flux/test_crystallization_robustness.py PASSES (T3 9/10 from R-1d-T3-bis stays robust)"
      - "tests/flux/test_decay.py PASSES (T4 unaffected)"
      - "tests/flux/test_learning.py PASSES (R-5 fast tests stay green; slow excluded)"
    time_budget_hours: 4
    status: None
    attempts: 1
    last_session: "2026-05-18T04:44:56.880665"
    blockers: ["Spec amendment §5.8 by Claude under user authorization 2026-05-17 23:00 — adds continuous activation field A(x,y,z,t) as plasticity-signal smoother. Discrete substrate primary; field is bookkeeping."]
    created: 2026-05-17
  - id: R-13
    title: bridge-weight spectrum — substrate-internal observable bypassing synthesis
    brief: docs/superpowers/plans/2026-05-19-flux-encoder-free-iter2.md
    preregistered_acceptance:
      - "tests/flux/test_bridge_spectrum.py::test_bridge_spectrum_observable_constructs PASSES (NEW: extract (freq, weight) pairs across all alive bridges, normalised histogram >= 32 bins populated)"
      - "tests/flux/test_bridge_spectrum.py::test_bridge_spectrum_zero_on_empty_substrate PASSES (NEW: 0-node substrate returns empty distribution, not NaN/error)"
      - "tests/flux/test_bridge_spectrum.py::test_bridge_spectrum_differs_under_different_audio PASSES (NEW: 50k-tick runs on English vs matched-RMS white noise must give KL > 0.1 between spectra)"
      - "tests/flux/test_conservation.py PASSES (T1 robust)"
      - "tests/flux/test_crystallization_robustness.py PASSES (T3 robust)"
    time_budget_hours: 4
    status: failed
    attempts: 3
    last_session: "2026-05-19T10:16:51"
    blockers: ["R-LR-1 diagnosed synthesis as the bottleneck; this item bypasses synthesis entirely with a substrate-internal observable", "main-sync force-set 2026-05-19: autopilot/R-13 branch reached 3-strike NULL terminal at attempt 3 (commit c4edbb2), then 6 charter-refused follow-on sessions (04c332f..831b548) failed to sync verdict to main. Manual force-set per branch state."]
    created: 2026-05-19

  - id: R-14
    title: synthesis sensitivity sweep — find Q/gain where firings dominate baseline
    brief: docs/superpowers/plans/2026-05-19-flux-encoder-free-iter2.md
    preregistered_acceptance:
      - "tests/flux/test_synthesis_sensitivity.py::test_synthesis_baseline_with_empty_substrate_has_low_energy PASSES (NEW: empty-substrate synthesis RMS < 0.1 * trained-substrate synthesis RMS)"
      - "tests/flux/test_synthesis_sensitivity.py::test_synthesis_with_firings_dominates_baseline PASSES (NEW: 100 firings/sec drive produces FFT peak at firing-pattern freq, not resonator natural freq)"
      - "Session picks smallest (Q, gain) from sweep [Q=3,5,10,30] x [gain=1,5,25,100] that passes both. Documents choice in phase-log"
      - "tests/flux/test_synthesis.py PASSES (F2 baseline synthesis unit tests still pass at original Q=10 gain=1 defaults; new combo is additional config)"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-19T15:45:08.594028"
    blockers: ["R-LR-1 diagnosed synthesis-baseline as the dominant signal in babble; this item tunes Q/gain so firings can break through"]
    created: 2026-05-19

  - id: R-15
    title: R-LR-8 infrastructure — salvage R-13 spectrum + add flux snapshot + wire tuned synthesis env
    brief: docs/superpowers/plans/2026-05-19-flux-encoder-free-iter2.md
    preregistered_acceptance:
      - "tests/flux/test_flux_snapshot.py::test_roundtrip_preserves_substrate_state PASSES (NEW: save flux Quanta+Nodes+Bridges+Grid+tick state to .npz, load back, verify all arrays bit-identical and substrate dynamics reproduce from loaded state)"
      - "tests/flux/test_flux_snapshot.py::test_snapshot_emits_at_configured_interval PASSES (NEW: with EQMOD_SNAPSHOT_EVERY_TICKS=100, encoder_free_training runner writes exactly 5 snapshot files after 500 ticks at the expected tick counts {100, 200, 300, 400, 500})"
      - "tests/flux/test_bridge_spectrum.py::test_bridge_spectrum_observable_constructs PASSES (salvaged from autopilot/R-13 commit eca9545 — R-13 NULLed on architectural firewall not function; the function itself passed at 50k-tick scope; session brings agent/flux/bridge_spectrum.py + corpus_spectrum.py + test file to main via git checkout autopilot/R-13 -- <paths>)"
      - "tests/flux/test_bridge_spectrum.py::test_bridge_spectrum_zero_on_empty_substrate PASSES (same salvage)"
      - "tests/flux/test_tuned_synthesis_env.py::test_env_loads_Q3_gain1 PASSES (NEW: with EQMOD_USE_TUNED_SYNTHESIS=1, agent.flux.synthesis.get_synthesis_config returns SynthesisConfig with Q=3.0 and impulse_gain=1.0, per R-14's locked sweep choice in commit e9c9275)"
      - "tests/flux/test_R_LR_8_acceptance.py is collected by pytest (NEW: file imports cleanly, defines test_bridge_spectrum_audio_vs_white_noise and test_synthesis_audio_vs_no_input with the pre-registered thresholds KL>0.5 and KL>0.001 from docs/superpowers/plans/2026-05-19-flux-encoder-free-iter2.md; tests skip gracefully when EQMOD_R_LR_8_OUT_DIR is unset, so the file collects cleanly under pytest before R-LR-8 has run; the file does NOT pass without R-LR-8's output — it scaffolds the long-run gate)"
      - "tests/flux/test_conservation.py PASSES (T1 robust under new snapshot code)"
      - "pytest -m 'not slow' PASSES (no regressions in the legacy or flux fast-test slice)"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-20T00:20:27.688452"
    blockers: ["R-14 passed (locked Q=3 gain=1 in commit e9c9275); session may reference R-14 phase-log for synthesis env conventions"]
    created: 2026-05-19

  - id: R-16
    title: architectural firewall diagnostic — 50k smoke for content-coupling in nodes.energy
    brief: docs/superpowers/plans/2026-05-19-flux-encoder-free-iter2.md
    preregistered_acceptance:
      - "tests/flux/test_firewall_diagnostic.py::test_50k_matched_rms_english_vs_white_noise_bridge_spectrum_KL_above_0p01 PASSES (NEW: run two encoder-free 50k-tick substrates with identical seed and identical RMS amplitude — one fed English audio segment from agent/flux/audio fixtures, one fed white noise of matched RMS — compute bridge spectra on the alive bridges of each, assert symmetric KL > 0.01. Threshold 0.01 chosen pre-data as the smallest non-trivial value above floating-point noise floor at 50k-tick scope.)"
      - "tests/flux/test_firewall_diagnostic.py::test_50k_matched_rms_english_vs_silence_bridge_spectrum_KL_above_0p1 PASSES (NEW: control test — English audio vs silence with same RMS-energy budget injected as noise floor; KL must exceed 0.1, because silence is the maximally different input. If THIS test also nulls, the firewall is total, not partial.)"
      - "LOGBOOK entry records both KL numbers and the audio fixtures used. If KL_english_vs_whitenoise < 0.01, the entry must explicitly name this as confirming R-13's architectural-firewall forensic finding (eca9545) and motivate the G24 amendment as the next item."
      - "tests/flux/test_conservation.py PASSES (T1 robust under whatever fixtures the session uses)"
      - "tests/flux/test_crystallization_robustness.py PASSES (T3 robust)"
    time_budget_hours: 4
    status: failed
    attempts: 1
    last_session: "2026-05-20T01:33:17.764176"
    blockers: ["R-15 must reach status=passed first (provides agent/flux/bridge_spectrum.py + corpus_spectrum.py)", "Quantifiziertes Architektur-Finding KL=0.0 — motiviert G24, kein retry sinnvoll (Triage 2026-05-21)"]
    created: 2026-05-19

  - id: R-17
    title: G24 amendment — implement energy-weighted flux pathway per docs/amendments/G24-energy-weighted-flux.md
    brief: docs/amendments/G24-energy-weighted-flux.md
    preregistered_acceptance:
      - "tests/flux/test_g24_amendment.py::test_count_energy_flux_through_sums_quanta_energy PASSES (NEW: synthetic substrate with 3 alive quanta of energies {1.0, 2.0, 4.0} all within r_flux of one alive bridge; count_energy_flux_through returns 7.0 for that slot, count_flux_through returns 3 for that slot; both arrays have shape (max_bridges,) — per amendment §3 row 1)"
      - "tests/flux/test_g24_amendment.py::test_count_energy_flux_through_zero_quanta_returns_zeros PASSES (NEW: 0 alive quanta returns a float array of all zeros, no NaN — amendment §3 row 2)"
      - "tests/flux/test_g24_amendment.py::test_apply_plasticity_energy_weighted_strengthens_proportionally PASSES (NEW: two bridges, energy_flux=10.0 and 2.0 both above flux_min; first bridge's weight gain is exactly 5x the second's after one apply — amendment §3 row 3)"
      - "tests/flux/test_g24_amendment.py::test_apply_plasticity_energy_weighted_decay_below_flux_min PASSES (NEW: bridge with energy_flux=0.0 and flux_min=1.0 decreases by lam*1.0; energy_flux=0.5 decreases by lam*0.5; energy_flux=1.5 increases linearly in gamma*1.5 — amendment §3 row 4)"
      - "tests/flux/test_g24_amendment.py::test_env_var_routes_to_weighted_path_in_encoder_free_training PASSES (NEW: with EQMOD_USE_ENERGY_WEIGHTED_FLUX=1, training_step calls count_energy_flux_through and apply_plasticity_energy_weighted, NOT the count-based versions; monkey-patch fixture asserts this — amendment §3 row 5)"
      - "tests/flux/test_g24_amendment.py::test_env_var_default_keeps_count_based_path PASSES (NEW: without the env var or set to '0', training_step calls the count-based functions; same monkey-patch fixture — amendment §3 row 6)"
      - "tests/flux/test_conservation.py PASSES (T1 robust under legacy count-based path — amendment §3 row 7)"
      - "tests/flux/test_crystallization_robustness.py PASSES (T3 robust under legacy count-based path — amendment §3 row 8)"
      - "tests/flux/test_bridge_spectrum.py PASSES (R-15 salvaged tests still green — amendment §3 row 9)"
      - "tests/flux/test_R_LR_8_acceptance.py is collected by pytest (R-15 scaffold still imports cleanly — amendment §3 row 10)"
      - "pytest -m 'not slow' PASSES (no regressions in fast-test slice — amendment §3 row 11)"
    time_budget_hours: 4
    status: failed
    attempts: 1
    last_session: "2026-05-20T19:15:19"
    blockers: ["R-15 must reach status=passed first (provides salvaged bridge_spectrum + R-LR-8 scaffold the G24 tests use as baseline). Architectural firewall was quantitatively confirmed by the prior diagnostic item (KL=0.000000 on both gates 2026-05-20T00:33Z, see LOGBOOK 2026-05-20 and amendment doc docs/amendments/G24-energy-weighted-flux.md frozen 2026-05-20 with locked pre-registration). Operator-administrative closure 2026-05-20T20:30+ at status=failed after attempt 1 NULLed on acceptance row 9 (test_bridge_spectrum_differs_under_different_audio: KL=0.0000, threshold 0.1) — that test is an architectural firewall-reproduction test that can only pass under EQMOD_USE_ENERGY_WEIGHTED_FLUX=1, which R-17 implements but does not activate. The mis-scoping is documented in docs/amendments/G24-energy-weighted-flux.md 'Pre-data correction record' section. R-17b queued with corrected acceptance scope; R-18 retargeted to R-17b."]
    created: 2026-05-20

  - id: R-17b
    title: G24 amendment — implement energy-weighted flux pathway (acceptance corrected after R-17 mis-scoping)
    brief: docs/amendments/G24-energy-weighted-flux.md
    preregistered_acceptance:
      - "tests/flux/test_g24_amendment.py::test_count_energy_flux_through_sums_quanta_energy PASSES (amendment §3 row 1)"
      - "tests/flux/test_g24_amendment.py::test_count_energy_flux_through_zero_quanta_returns_zeros PASSES (amendment §3 row 2)"
      - "tests/flux/test_g24_amendment.py::test_apply_plasticity_energy_weighted_strengthens_proportionally PASSES (amendment §3 row 3)"
      - "tests/flux/test_g24_amendment.py::test_apply_plasticity_energy_weighted_decay_below_flux_min PASSES (amendment §3 row 4)"
      - "tests/flux/test_g24_amendment.py::test_env_var_routes_to_weighted_path_in_encoder_free_training PASSES (amendment §3 row 5)"
      - "tests/flux/test_g24_amendment.py::test_env_var_default_keeps_count_based_path PASSES (amendment §3 row 6)"
      - "tests/flux/test_conservation.py PASSES (T1 robust under legacy count-based path — amendment §3 row 7)"
      - "tests/flux/test_crystallization_robustness.py PASSES (T3 robust under legacy count-based path — amendment §3 row 8)"
      - "tests/flux/test_bridge_spectrum.py::test_bridge_spectrum_observable_constructs PASSES (R-15 salvaged mechanical test — corrected scope per G24 amendment 'Pre-data correction record' 2026-05-20; the third test in the file, test_bridge_spectrum_differs_under_different_audio, is the content-coupling verification that lives in R-18 §4 row 1)"
      - "tests/flux/test_bridge_spectrum.py::test_bridge_spectrum_zero_on_empty_substrate PASSES (R-15 salvaged mechanical test — corrected scope per G24 amendment 'Pre-data correction record' 2026-05-20)"
      - "tests/flux/test_R_LR_8_acceptance.py is collected by pytest (R-15 scaffold still imports cleanly — amendment §3 row 10)"
      - "tests/flux/test_g24_amendment.py PASSES (full file — 6 tests, sanity check the file has no NEW tests added that drift from the 6 mechanic items above)"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-20T22:35:40.776347"
    blockers: ["R-15 must reach status=passed first. See docs/amendments/G24-energy-weighted-flux.md 'Pre-data correction record' 2026-05-20 for why the prior G24-implementation item was administratively closed and this item replaces it. Reference G24 implementation lives at commit aad8c53; this session may import that code via git checkout rather than re-implement, then run the corrected 12-test acceptance suite."]
    created: 2026-05-20

  - id: R-18
    title: G24 verification — 50k smoke that energy-weighted path breaks the firewall
    brief: docs/amendments/G24-energy-weighted-flux.md
    preregistered_acceptance:
      - "tests/flux/test_g24_verification.py::test_50k_english_vs_white_noise_KL_above_0p01_under_weighted_path PASSES (NEW: two encoder-free 50k-tick substrates, identical seed, matched RMS budget, English vs white noise, both with EQMOD_USE_ENERGY_WEIGHTED_FLUX=1; symmetric KL of bridge-weight spectra > 0.01 — threshold identical to R-16's failed gate, amendment §4 row 1)"
      - "tests/flux/test_g24_verification.py::test_50k_english_vs_silence_KL_above_0p1_under_weighted_path PASSES (NEW: same config but English vs silence-with-matched-RMS-noise-floor; KL > 0.1 — threshold identical to R-16's failed gate, amendment §4 row 2)"
      - "tests/flux/test_g24_verification.py::test_50k_white_noise_vs_white_noise_independent_seeds_KL_below_0p005_under_weighted_path PASSES (NEW: negative control — two white-noise substrates with INDEPENDENT seeds but same audio statistics; KL between their bridge spectra < 0.005; amendment §4 row 3)"
      - "tests/flux/test_g24_verification.py::test_legacy_count_based_path_still_returns_KL_zero PASSES (NEW: same 50k English-vs-white-noise pair WITHOUT the env var; KL < 1e-6, reproduces R-16. Amendment §5 control: confirms R-17 did not regress the legacy path.)"
      - "tests/flux/test_conservation.py PASSES (T1 robust)"
      - "tests/flux/test_crystallization_robustness.py PASSES (T3 robust)"
      - "LOGBOOK entry records all four KL numbers + the random seeds used. If KL_english_vs_whitenoise > 0.01 AND KL_english_vs_silence > 0.1, the session must also append a note recommending R-LR-9 as the next long-run item (1.8M-tick verification of G24)."
    time_budget_hours: 4
    status: None
    attempts: 1
    last_session: "2026-05-21T00:36:01.215133"
    blockers: ["R-17b must reach status=passed first (provides count_energy_flux_through + apply_plasticity_energy_weighted; the prior G24-implementation item was closed administratively at failed after acceptance-row-9 mis-scoping, recorded in docs/amendments/G24-energy-weighted-flux.md 'Pre-data correction record' 2026-05-20)"]
    created: 2026-05-20

  - id: R-19
    title: pipeline smoke test — third defence layer against silent autopilot failures
    brief: LOGBOOK.md
    preregistered_acceptance:
      - "tests/test_pipeline_smoke.py::test_preflight_picks_only_unblocked_items PASSES (NEW: build a minimal mock-queue with one item whose blocker mentions a passed item, one whose blocker mentions a null item, one whose blocker mentions a queued item; assert preflight picks the third — chain-prerequisite — and that the first two are flagged correctly by validate_queue.py)"
      - "tests/test_pipeline_smoke.py::test_preflight_rejection_message_enumerates_blocked_items PASSES (NEW: build a mock-queue where every queued item is blocked by something terminal-non-passed; capture preflight stderr; assert it contains the specific blocked-by-X-status-Y enumeration the 0e2c0f6 commit added)"
      - "tests/test_pipeline_smoke.py::test_validate_queue_blocks_terminal_blocker_mention PASSES (NEW: synthetic QUEUE.yaml with a queued item whose blocker mentions a null/failed item; assert validate_queue.py exits 1 with the offender's id in stderr)"
      - "tests/test_pipeline_smoke.py::test_validate_queue_allows_queued_chain PASSES (NEW: synthetic QUEUE.yaml with R-A queued and R-B blocking on 'R-A must pass'; assert validate_queue.py exits 0 — legitimate forward chain)"
      - "tests/test_pipeline_smoke.py::test_stagnation_detector_sets_stop_after_threshold PASSES (NEW: monkey-patch measure_progress_signal to return constant value across 4 invocations; assert STOP file is set after invocation 4, LOGBOOK appended, alerted flag in stagnation_state.json is True)"
      - "tests/test_pipeline_smoke.py::test_stagnation_detector_resets_on_progress PASSES (NEW: invoke twice with stagnant signal, then once with changed signal; assert consecutive_stagnant_ticks resets to 0, STOP NOT set, alerted False)"
      - "tests/test_pipeline_smoke.py::test_pre_commit_hook_rejects_self_blocking_queue_edit PASSES (NEW: write a synthetic QUEUE.yaml with R-X-blocked-by-mention-of-null-item to a tempdir, stage it, run the hook script via subprocess, assert exit 1)"
      - "tests/test_pipeline_smoke.py is added to pytest -m 'not slow' default run so it executes on every CI invocation and every postflight regression check"
      - "pytest -m 'not slow' PASSES (no regressions in the rest of the fast slice)"
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-21T01:42:27.926354"
    blockers: ["R-15 must reach status=passed first (the pipeline test must be able to import the bridge_spectrum and snapshot machinery R-15 salvaged in case those modules are loaded by the same pytest run)"]
    created: 2026-05-20


  - id: R-20
    title: G24 energy-variance diagnostic — does quanta.energy actually vary across audio inputs
    brief: docs/amendments/G24-energy-weighted-flux.md
    preregistered_acceptance:
      - "tests/flux/test_g24_diagnostic.py::test_quanta_energy_histogram_differs_under_english_vs_whitenoise PASSES (NEW: two 10k-tick encoder-free substrates with EQMOD_USE_ENERGY_WEIGHTED_FLUX=1, matched RMS, English vs white noise; compute histograms of quanta.energy[quanta.alive] at tick 10000; assert symmetric KL > 0.01 between them. Threshold 0.01 mirrors R-16/R-18.)"
      - "tests/flux/test_g24_diagnostic.py::test_quanta_energy_histogram_negative_control_same_audio_different_seed PASSES (NEW: same English audio fed to two substrates with different RNG seeds; KL between their quanta.energy histograms < 0.005. Confirms variance comes from audio content, not seed dispersion.)"
      - "tests/flux/test_g24_diagnostic.py::test_bridge_energy_flux_array_differs_under_english_vs_whitenoise PASSES (NEW: at tick 10000 of the two substrates from test 1, compute count_energy_flux_through; assert KL between the two flux-count arrays > 0.01. Isolates whether variance survives the bridge-readout step.)"
      - "tests/flux/test_conservation.py PASSES (T1 robust)"
      - "tests/flux/test_crystallization_robustness.py PASSES (T3 robust)"
      - "LOGBOOK entry with three KL numbers + verdict mapping: if test1 PASSES (energy varies) and test3 PASSES (flux varies) -> G25 should focus on PLASTICITY-RULE design. If test1 PASSES but test3 FAILS -> bridge geometry averages variance away -> G25 should rethink bridge-readout. If test1 FAILS -> energy itself does not vary across audio content -> upstream injection is the firewall -> G25 must redesign inject_raw_audio_sample."
    time_budget_hours: 4
    status: None
    attempts: 1
    last_session: "2026-05-21T02:47:19.018969"
    blockers: ["R-15 must reach status=passed first (provides agent/flux/bridge_spectrum.py + R-LR-8 scaffold the diagnostic test imports)"]
    created: 2026-05-21

  - id: R-21
    title: G25 amendment — content-driven xy-position in audio injection (combined impl + 10k verification)
    brief: docs/amendments/G25-content-driven-injection.md
    preregistered_acceptance:
      - "tests/flux/test_g25_amendment.py::test_content_driven_position_hash_returns_different_xy_for_different_sample_values PASSES (NEW: same sample_index=12345 with sample_value=0.1 vs 0.8 returns xy whose Euclidean distance > 0.5 * voxel_size — amendment §3 row 1)"
      - "tests/flux/test_g25_amendment.py::test_content_driven_position_hash_deterministic PASSES (NEW: same (sample_index, sample_value, seed) returns bit-identical xy — amendment §3 row 2)"
      - "tests/flux/test_g25_amendment.py::test_legacy_position_hash_unchanged PASSES (NEW: 4 pinned (sample_index, Lx, Ly, voxel_size) fixtures return their R-13/R-14-era xy values — amendment §3 row 3)"
      - "tests/flux/test_g25_amendment.py::test_env_var_routes_to_content_driven_path PASSES (NEW: with EQMOD_USE_CONTENT_DRIVEN_POSITION=1, inject_raw_audio_sample calls position_hash_content_driven via monkey-patch fixture — amendment §3 row 4)"
      - "tests/flux/test_g25_verification.py::test_10k_quanta_position_KL_above_0p1_english_vs_whitenoise PASSES (NEW: 2D position histogram of alive quanta at tick 10000, two encoder-free substrates with EQMOD_USE_CONTENT_DRIVEN_POSITION=1, matched RMS English vs white noise; symmetric KL > 0.1. 10x stronger than R-20's failed 0.01 threshold because position is higher-bandwidth than amplitude. Threshold locked pre-data — amendment §3 row 5)"
      - "tests/flux/test_g25_verification.py::test_10k_negative_control_same_audio_different_seed_position_KL_below_0p005 PASSES (NEW: same English audio two seeds, position-histogram KL < 0.005 — amendment §3 row 6)"
      - "tests/flux/test_g25_verification.py::test_10k_bridge_count_differs_under_english_vs_whitenoise PASSES (NEW: at tick 10000 of test-5 substrates, bridge-weight spectra KL > 0.01 — amendment §3 row 7)"
      - "tests/flux/test_conservation.py PASSES (T1 robust)"
      - "tests/flux/test_crystallization_robustness.py PASSES (T3 robust)"
      - "LOGBOOK entry records position-KL (test 5), neg-control-KL (test 6), bridge-spectrum-KL (test 7). If all three PASS, the entry must recommend R-LR-10 as next long-run for 1.8M-tick verification under G25."
    time_budget_hours: 4
    status: None
    attempts: 1
    last_session: "2026-05-21T12:01:25.435527"
    blockers: ["R-15 must reach status=passed first (provides bridge_spectrum + R-LR-8 scaffold). R-20 confirmed content-coupling at the energy field is insufficient (KL=0.005, 10x below R-20 threshold 0.01); G25 adds content-coupling at the position field per pre-registered branch (c) in docs/amendments/G24-energy-weighted-flux.md Pre-data correction record"]
    created: 2026-05-21

  - id: R-22
    title: G26 amendment — density-by-amplitude injection (combined impl + 10k verification, slot 3 of 3 in iteration cap)
    brief: docs/amendments/G26-density-by-amplitude.md
    preregistered_acceptance:
      - "tests/flux/test_g26_amendment.py::test_density_count_proportional_to_amplitude PASSES (NEW: with DENSITY_K=4 DENSITY_N_MAX=4, n(0.0)=0, n(0.125)=1, n(0.4)=2, n(0.7)=3, n(1.0)=4 — amendment §3 row 1)"
      - "tests/flux/test_g26_amendment.py::test_density_preserves_total_energy_per_sample PASSES (NEW: sample_value=0.6 with density mode injects 2 quanta with energy=0.3 each, sum = abs(sample_value) by construction across all n>=1 — amendment §3 row 2)"
      - "tests/flux/test_g26_amendment.py::test_legacy_injection_unchanged PASSES (NEW: without env var, inject_raw_audio_sample returns 1 quantum with legacy position_hash xy and energy=abs(sample_value) bit-identical to 4 pinned R-13/R-14 fixtures — amendment §3 row 3)"
      - "tests/flux/test_g26_amendment.py::test_env_var_routes_to_density_path PASSES (NEW: with EQMOD_USE_DENSITY_BY_AMPLITUDE=1, inject_raw_audio_sample calls density path via monkey-patch — amendment §3 row 4)"
      - "tests/flux/test_g26_verification.py::test_10k_alive_quanta_count_differs_under_english_vs_whitenoise PASSES (NEW: two 10k-tick encoder-free substrates with EQMOD_USE_DENSITY_BY_AMPLITUDE=1, matched RMS English vs white noise; alive-quanta-count histograms KL > 0.05 at tick 10000. Threshold 0.05 halved from R-20's 0.01-energy threshold because count is lower-bandwidth than energy but preserves much larger fraction of variance through mixing — amendment §3 row 5)"
      - "tests/flux/test_g26_verification.py::test_10k_alive_quanta_count_negative_control_same_audio_diff_seed PASSES (NEW: same English audio two seeds, count-histogram KL < 0.01. Content-vs-seed discriminator — amendment §3 row 6)"
      - "tests/flux/test_g26_verification.py::test_10k_bridge_count_differs_under_english_vs_whitenoise PASSES (NEW: at tick 10000 of test-5 substrates, total alive-bridge count differs > 10% between EN and WN OR bridge-spectrum KL > 0.01. OR semantics — density may propagate via count-channel or weight-channel — amendment §3 row 7)"
      - "tests/flux/test_conservation.py PASSES (T1 robust; energy-per-sample invariant by construction via test 2 — amendment §3 row 8)"
      - "tests/flux/test_crystallization_robustness.py PASSES (T3 robust — amendment §3 row 9)"
      - "tests/flux/test_audio_raw_injection.py PASSES (R-10/R-11 legacy injection tests still green — amendment §3 row 10)"
      - "LOGBOOK entry records count-KL (test 5), neg-control-KL (test 6), bridge-count delta (test 7), peak Quanta-buffer fill from one verification run. If all three primary content tests PASS, must recommend R-LR-11 as next long-run for 1.8M-tick G26 verification — amendment §3 row 11"
    time_budget_hours: 4
    status: None
    attempts: 1
    last_session: "2026-05-21T20:59:25.922721"
    blockers: ["R-15 must reach status=passed first (provides bridge_spectrum + R-LR-8 scaffold + test fixtures). G24 + G25 NULLed on content-coupling (R-18 + R-20 + R-21); R-21 LOGBOOK 2026-05-21T11:50 self-recommended density-by-amplitude as G26 because count is substrate-conserved (not exposed to the mixing-time low-pass filter that washed energy and position out). Iteration-cap slot 3 of 3 per LOGBOOK 2026-05-20 pre-registration; after R-22 NULL pivot to G20-G23 fires"]
    created: 2026-05-21

  - id: R-22b
    title: G26 amendment with K=8 (pre-data correction after R-22 NULL — operator decision 2026-05-22)
    brief: docs/amendments/G26-density-by-amplitude.md
    preregistered_acceptance:
      - "tests/flux/test_g26_amendment.py::test_density_count_proportional_to_amplitude PASSES (corrected fixtures: with DENSITY_K=8 DENSITY_N_MAX=8, n(0.0)=0, n(0.0625)=1, n(0.1875)=2, n(0.4)=3, n(0.7)=6, n(1.0)=8 — amendment Pre-data correction record 2026-05-22)"
      - "tests/flux/test_g26_amendment.py::test_density_preserves_total_energy_per_sample PASSES (sample_value=0.6 with K=8 density mode injects 5 quanta with energy=0.12 each, sum = abs(sample_value) by construction)"
      - "tests/flux/test_g26_amendment.py::test_legacy_injection_unchanged PASSES (without env var, bit-identical to 4 pinned R-13/R-14 fixtures — unchanged from R-22)"
      - "tests/flux/test_g26_amendment.py::test_env_var_routes_to_density_path PASSES (with EQMOD_USE_DENSITY_BY_AMPLITUDE=1, calls density path — unchanged from R-22)"
      - "tests/flux/test_g26_verification.py::test_10k_alive_quanta_count_differs_under_english_vs_whitenoise PASSES (NEW: same 10k-tick + matched-RMS setup as R-22, but DENSITY_K=8 → zero-injection boundary moves from |x|<0.125 to |x|<0.0625; symmetric count-KL > 0.05)"
      - "tests/flux/test_g26_verification.py::test_10k_alive_quanta_count_negative_control_same_audio_diff_seed PASSES (same English audio two seeds, count-histogram KL < 0.01; with K=8 substrate is non-degenerate so this test is meaningful (not trivially-PASS-by-substrate-dead like R-22's test 6))"
      - "tests/flux/test_g26_verification.py::test_10k_bridge_count_differs_under_english_vs_whitenoise PASSES (bridge-count delta > 10% OR bridge-spectrum KL > 0.01)"
      - "tests/flux/test_conservation.py PASSES (T1 robust)"
      - "tests/flux/test_crystallization_robustness.py PASSES (T3 robust)"
      - "tests/flux/test_audio_raw_injection.py PASSES (R-10/R-11 legacy injection tests still green)"
      - "LOGBOOK entry records the four KL numbers + peak Quanta-buffer fill + actual measured frac(|x|<0.0625) for the English corpus (predicted ~25%, vs R-22's 64.55% under K=4). If all three primary content tests PASS, must recommend R-LR-11 as next long-run for 1.8M-tick G26 verification"
    time_budget_hours: 4
    status: None
    attempts: 1
    last_session: "2026-05-22T02:18:47.358570"
    blockers: ["R-15 must reach status=passed first (provides bridge_spectrum + R-LR-8 scaffold + test fixtures). R-22b is the K-corrected iteration of R-22; the pre-data K correction is documented in docs/amendments/G26-density-by-amplitude.md Pre-data correction record 2026-05-22 (analog marker_protocol.md marker-5 correction pattern: parameter-not-threshold, before-data, construct-under-test-unchanged). R-22 + R-22b together exhaust slot 3 of 3 in the G24-G26 iteration cap; after R-22b NULL the pivot to G20-G23 fires per LOGBOOK 2026-05-20"]
    created: 2026-05-22

  - id: R-23
    title: bet-infrastructure — 1h-iteration loop + separate queue + dispatcher per LOGBOOK 2026-05-22 bet pre-registration
    brief: LOGBOOK.md
    preregistered_acceptance:
      - "tools/bet_dispatcher.py exists and runs as a standalone script, polling ~/.eqmod/bet/queue.yaml every 60 seconds (configurable via --interval), launching one bet hypothesis at a time, enforcing a 1h hard cap per hypothesis-iteration via SIGTERM/SIGKILL (mirrors tools/long_run_dispatcher.py hard-cap pattern), evaluating verdict from result.json + per-bet pytest target, writing per-iteration LOGBOOK entry, persisting status into the bet queue."
      - "~/.eqmod/bet/queue.yaml schema documented in tools/bet_dispatcher.py module docstring with at least these fields per item: id, hypothesis (one paragraph), references (list of papers/textbook chapters being verknuepft), pytest_target, status (queued/running/passed/null/failed), attempts, max_runtime_seconds (default 3600), created_at, finished_at."
      - "tests/test_bet_dispatcher.py::test_dispatcher_launches_queued_item PASSES (NEW: mock-queue with one queued item, dispatcher launches it via subprocess, writes pidfile)"
      - "tests/test_bet_dispatcher.py::test_dispatcher_hard_cap_kills_overrun PASSES (NEW: mock item that sleeps 5 seconds with max_runtime_seconds=2; dispatcher SIGKILLs it after 2s, marks status=failed with hard-cap reason)"
      - "tests/test_bet_dispatcher.py::test_dispatcher_evaluates_passed_item PASSES (NEW: mock item that exits 0 with PASSED result.json; dispatcher reads it, marks status=passed, writes LOGBOOK entry)"
      - "tests/test_bet_dispatcher.py::test_dispatcher_evaluates_null_item PASSES (NEW: mock item that exits 0 with NULL result.json; dispatcher marks status=null, writes LOGBOOK entry)"
      - "tools/validate_bet_queue.py exists and applies the same explicit-dependency semantics (queue_semantics.DEPENDENCY_RE) as tools/validate_queue.py; smoke-tested with synthetic queues."
      - "tests/test_bet_dispatcher.py PASSES in pytest -m 'not slow' (no new slow tests added; full bet pipeline-smoke fits in <30s wallclock)"
      - "docs/bet/README.md exists with a one-page operator manual: how to add a hypothesis, expected iteration cadence, where the per-iteration LOGBOOK lives, how to read /results in Telegram for bet items, what 'PASS / NULL / FAILED' mean for a 1h iteration vs a 4h amendment item."
      - "Hypothesis catalogue ~/.eqmod/bet/hypothesis_catalogue.md exists with at least 10 candidate hypotheses, each one paragraph + a list of references being verknuepft. Catalogue is the operator-readable plan for the first 2 weeks of bet iterations."
      - "Telegram /bet command added to tools/notify_telegram_receiver.py: returns the current bet queue state (X queued / Y running / Z passed / N null / M failed) + last 3 completed iterations + win-condition progress (how many of T1-T5 have ever been passed in a single iteration). Smoke-tested by sending /bet from Telegram."
    time_budget_hours: 4
    status: passed
    attempts: 1
    last_session: "2026-05-23T00:26:31.700198"
    blockers: ["R-15 must reach status=passed first (provides bridge_spectrum, R-LR-8 scaffold, and the test infrastructure pattern). R-23 sets up the parallel bet pipeline; it does NOT touch the existing G-amendment autopilot pipeline, which continues to manage long-run dispatcher items (R-LR-4, R-LR-5..8). The bet pre-registration is in LOGBOOK.md 2026-05-22 with pre-data constraint correction recorded same day."]
    created: 2026-05-22
