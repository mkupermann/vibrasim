# G141 — The physical paradigm can LEARN (Boltzmann machine = stochastic Ising)

## Result
Planted an 8-node Boltzmann distribution, drew data, then LEARNED W,b by the Boltzmann rule
(data-correlation − model-correlation). Mean |pairwise-correlation| error of the learned model's samples
vs the data: **0.018** (random baseline 0.708). **VERDICT: PASS** — the model learned the distribution.

## Finding — the buildable physical paradigm does memory + optimization + LEARNING
The SAME coupled substrate that optimizes (G138/9, Ising) and recalls (G140, Hopfield), run STOCHASTICALLY,
is a Boltzmann machine that LEARNS a distribution unsupervised — no LLM, no transformer. So the no-LLM
physical-AI primitive set is complete: store/recall + optimize + learn. Established methods (Boltzmann
machine / Ising / Hopfield), named as such; the contribution is only the honest, working reference for the
project's physical-computing goal. (Boltzmann learning is slow and small-scale — a real ceiling — but it
IS learning, which EQMOD's own physics cannot do, G132.)
