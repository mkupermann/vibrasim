# BP-C100 — Free dual talent with self_modify_rate (never tried)

**PRE-REGISTERED 2026-07-26 before data**  
**Depends on:** free dual NULL farm C27–C99; C52 self_aware free dual NULL; `self_modify_rate` never BP free dual treatment  
**Discipline:** **new mechanism** = self_aware ON + elevated self_modify_rate free dual + wall vs default rate. Budget-fit T=500 N=250. n_emit=0.

## Hypothesis

Wall ON. Neuron dynamics ON. Both arms `self_aware_enabled=True`, `self_modify_enabled=True`.  
Treatment: `self_modify_rate=0.2`.  
Control: `self_modify_rate=0.05` (default).

B1 treat ordered ≥0.90 · B2 ctrl ≤0.80 · B3 treat pop ≥0.80 · B4 delta ≥0.15

## Bars

| id | criterion | threshold |
|----|-----------|-----------|
| B1 | treat ordered fraction | ≥0.90 |
| B2 | ctrl ordered fraction | ≤0.80 |
| B3 | treat dual-side L4+ pop | ≥0.80 |
| B4 | B1−B2 | ≥0.15 |

Seeds {7781,7791} trials 2. T=500. Budget ~8 min, hard cap 16 min.

## Prediction

🔮 LEAN NULL. Self-modify needs prediction-error / pattern machinery free dual inject does not provide for decade unlock.

## RESULT

*(filled after run)*
