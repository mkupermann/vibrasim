# G173 — bandwidth with domain-checked specificity and a distance law

**Status: SIGNED OFF 2026-08-13 (round 13: A, C, E for xviii with A's
distance-law and C's domain-check conditions authored-in; D JA after the
declared-subdomain fix of his weak-decoy objection; the domain walk caught
a distance-2 spec ghost in the law wording before commit) — committed
before any data (D2). Bars final per D3.**
**Verdict 2026-08-13: INCONCLUSIVE** (SCRAM-X 0.604 > 0.6) — and decisive:
decoy ≡ treatment per-seed, because span-preserving arrangement permutations
leave centered-chain ENDS fixed → cross rests physically identical (spec
degeneration #3, structurally inert decoy). The sub-full-anchor channel is
span-only; content association exists exactly at full anchoring (K12 1.000,
4× replicated). The graded bandwidth law is refuted; the channel is a STEP
FUNCTION. Association-on-registers closes by CHARACTERIZATION. LOGBOOK
2026-08-13.

## 1. The one question (D1)

> On writability-clean geometry (G172 Stage 1, reusable), with specificity
> controls PROVEN non-degenerate over their domain (docs/patterns/
> control_spec_nondegeneracy.md), does association through the
> cross-structure follow researcher A's registered DISTANCE LAW — per-bit
> reconstruction falls monotonically with bond-distance to the nearest
> cross-anchor — and the k=4 < k=6 bandwidth ordering?

## 2. Domain check (researcher C's condition — written before any code)

- **Arrangement decoys** (same chain, same span, different bit order) exist
  iff the chain has mixed weight. Uniform-weight chains admit none.
- **bits_per = 1 (k=12): content ≡ span — no span-neutral content decoy
  exists at all.** The k=12 arm is declared **SPECIFICITY-NON-CERTIFIABLE
  by construction** and serves ONLY as the sensitivity anchor (its 1.000
  is already thrice-replicated). No specificity claim attaches to it.
- Certifiable arms: **k=4** (m=2, 3-bit chains) and **k=6** (m=3, 2-bit
  chains), on a **DECLARED SUBDOMAIN** (sceptic D's condition, fixing the
  weak-decoy density risk before the run): pattern draws for these arms are
  constrained so EVERY chain is mixed-weight — each chain then admits an
  arrangement decoy by construction, decoy strength ≥ m ≥ 2 guaranteed.
  The distance/bandwidth claims apply to this declared subdomain, not to
  the full pattern space (the restriction is registered, not silent).
  WEAK-DECOY labeling remains as a safety net and must come up empty.

## 3. Protocol

G172 Stage-2 protocol (centered chains, Δy=8, censuses, boundary gate,
scramble centered-uniform, 8 pattern-pairs × seeds {42,7,13}) with:
- Arms k=4, k=6 (certifiable) + k=12 (sensitivity anchor, labeled).
- **SCRAM-X (per arm, k=4 and k=6):** cross-structure written from the
  arrangement-decoy geometry; must sit ≤ 0.6 and < 0.75 on
  strength-≥2 runs BEFORE treatment values count (dual gate).
- **NEG** (cross deleted): ≤ 0.6.
- **Per-bit anchor-distance recording:** every decoded bit is logged with
  its bond-distance to the nearest cross-anchored carrier (0 = adjacent).

## 4. Pre-registered distance law (researcher A; the primary verdict axis)

Ordinal, fixed now — DOMAIN-CHECKED (the first draft referenced distance-2
bits that NO arm produces; caught by the non-degeneracy walk before commit):
bits_per=3 yields anchor distances {0,1,0}, bits_per=2 only {0,0}. The
realizable law: mean reconstruction of distance-0 bits > distance-1 bits,
STRICT, on the k=4 arm (the only arm with both classes), on ≥ 2/3 seeds;
k=6 contributes distance-0 bits and the bandwidth ordering only. The decay
ratio is REPORTED; only the order gates. A curve tuned to G172's observed 0.618
would be a lowered bar — the law is an order, not a constant.

**Verdicts:**
- **DISTANCE-LAW CONFIRMED:** d0 > d1 strict on the k=4 arm on ≥ 2/3
  seeds AND k=4 mean < k=6 mean (bandwidth ordering) AND dual gates pass.
- **LAW-INVERTED / MIXED:** order violated with gates clean — the elastic
  model is wrong; record the observed order, stop the line's derivation
  claims.
- **INCONCLUSIVE:** dual-gate violation (SCRAM-X > 0.6 on strength-≥2 runs;
  k=12 < 0.90), census/boundary breaks, or > 50% WEAK-DECOY runs in a
  certifiable arm.
- **FAIL:** NEG or SCRAM-X ≥ 0.75, or Stage-1-passed geometry produces
  invalid censuses.

## 5. Predictions (calibration, before data)

- Distance order holds: 70% (spring-sphere reasoning: anchored ends are
  position-constrained to sphere intersections; the further a spacing sits
  from any anchor, the closer its carriers are to free-uniform).
- k=4 < k=6: 60%.
- Verdict: DISTANCE-LAW CONFIRMED 55%, LAW-INVERTED/MIXED 20%,
  INCONCLUSIVE 15%, FAIL 10% (weak-decoy risk removed by the declared
  subdomain).
- Most-likely failure mode: LAW-INVERTED at k=4 — the middle bit of a
  3-bit chain may inherit MORE anchor pull than the elastic order expects
  (both anchors reach it through one intermediate).

## 6. Budget

Harness delta (arrangement decoys + distance logging + labels): 40 min.
Compute ≈ 30 min. Verdict + LOGBOOK + FRONTIER (D10): 30 min.
**Realistic 2 h → hard cap 4 h.**

## 7. Out of scope

k=12 specificity (non-certifiable by construction), retention of
associations, kinematics dossier, new decoy families beyond arrangement.
