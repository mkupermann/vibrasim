# JEP-192 — functional grounding from OBSERVED INTERACTIONS (demonstrating the mechanism)

## Prediction (locked BEFORE run) [predict-calibrate]
- 🔮 affordances extracted from observed interaction sequences recover functional categories (seat vs surface)
  despite differing appearances — demonstrating functional grounding from interaction OBSERVATION; residual gap is
  real embodied interaction perception. HIT expected.

## Result — PASS (HIT) + an incidental mass-noun caveat
From a synthetic but principled USAGE LOG (an agent uses objects: (object, action) events, 10% noisy), extracted
per-object affordance profiles (observed action frequencies) — NO appearance, NO given affordance features — and
clustered: functional purity 1.00 (armchair+stool -> seat; desk+small_table -> surface), exactly the categories
APPEARANCE cannot recover (JEP-191, 0.50). Grounded the discovered functional categories with prose and reasoned:
'is a stool the same kind as an armchair (both seat)?' -> True. So FUNCTIONAL GROUNDING is DEMONSTRABLE from
interaction OBSERVATION (the JEP-62 mechanism) — observing how things are USED recovers function that static
appearance blurs. This moves functional grounding from 'characterized as blocked' (JEP-191) to 'demonstrated from
observed interaction'; the residual gap is REAL embodied interaction perception (here the log is synthetic, not
sensed). INCIDENTAL CAVEAT surfaced: 'is a stool furniture?' -> False, because 'furniture' is a MASS NOUN rejected by
read()'s plural-noun predicate heuristic ('A seat is furniture' not extracted — furniture has no article and no
plural -s, so it looks like an adjective). Same family as 'tiredness'; a real but separate recall limitation on mass
nouns (furniture/water/equipment/information). Prediction HIT; tally 81/108. Established (affordance grounding from
observed interactions, JEP-62; usage-profile clustering); named; no novelty.
