# JEP-93 — engine tier 2: Boolean-composed comprehension (AND/OR/NOT) — Boole's actual subject

## Why
Extend the 100% UnderstandingEngine one developmental tier with logical connectives (AND/OR/NOT over atomic is-a/
relation clauses) — thematically Boole's "Laws of Thought". Single connective + negation in contract; mixed
and/or precedence deliberately out of contract (a later tier).

## Prediction (locked BEFORE run) [predict-calibrate skill]
- 🔮 100% on a single-connective + negation battery. MOST-LIKELY MISS: NOT-parsing — the bare IS-A regex on
  "a poodle is not a fish" would capture "not" as the object (same surface-form class as JEP-92). Mitigated by
  handling negation explicitly BEFORE atomic parse (lesson applied from PREDICTION_LOG). Enumerated forms: "is
  not a", "does not", connective and/or, articles. Predict 100% within contract.

## Acceptance
- PASS: battery = 100%. Engine tier 2 holds 100% while adding Boolean composition.
- Established (Boolean logic over predicates, VSA atoms), named; no novelty.

## Calibration (after) — HIT
🔮 predicted 100% with NOT-parsing as the most-likely miss; APPLIED the logged lesson (handle negation before
atomic parse, enumerate surface forms). ACTUAL 12/12 = 100%. HIT — the prediction held because the prior misses'
lesson was applied rather than re-discovered. Calibration tally now 2/4.

## Result — PASS (100%)
Boolean battery 12/12 = 100%. Engine tier 2 adds AND/OR/NOT over atomic is-a/relation clauses (negation handled
explicitly pre-parse). Contract: single connective + negation; mixed and/or precedence is a later tier. Established
(Boolean logic over predicates, VSA atoms), named; no novelty.
