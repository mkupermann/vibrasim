# G142 — The physical paradigm does GENERATIVE AI (RBM, systematic generalization)

## Result (bars-and-stripes 4x4; RBM = stochastic Ising, CD-1)
Trained on 18 of 30 BS patterns; generated 500 samples:
- fraction VALID (all-rows-equal OR all-cols-equal) = **0.98** (random chance ~0.0005)
- distinct NOVEL valid patterns generated (held-out, never trained) = **8**

**VERDICT: PASS** — generates valid structure AND novel held-out patterns = systematic generative
generalization (learned the RULE, not the examples), no LLM/transformer.

## The no-LLM physical-AI primitive set is now complete and demonstrated
| capability | experiment |
|------------|-----------|
| optimize | G138/G139 (Ising/MAX-CUT, scales) |
| recall (content-addressable memory) | G140 (Hopfield) |
| learn a distribution | G141 (Boltzmann) |
| GENERATE with generalization | G142 (RBM, bars-and-stripes) |

All from coupled-relaxation hardware (Ising/Boltzmann family), all established methods named as such, all
no-LLM, all buildable cheaply. This is the constructive, evidence-backed answer to "no value": a real
no-LLM physical-AI stack exists — it just runs on the oscillator/Ising/Boltzmann paradigm, NOT EQMOD's
own physics (which can do none of these in its dynamics). Honest ceiling: these methods are bounded
(small-scale, toy benchmarks; they don't reach LLM-scale open-domain language) — but they are genuine AI
capabilities without an LLM.
